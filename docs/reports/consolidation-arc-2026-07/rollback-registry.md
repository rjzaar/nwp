# Consolidation Arc — Rollback Registry

One row per checkpoint. To reverse a checkpoint, run its **Restore** command.

| # | When | Checkpoint | Artifact(s) + sha | Git ref / bundle | Restore command |
|---|------|-----------|-------------------|------------------|-----------------|
| CP0 | 2026-07-24 | arc-baseline (tool state) | — | tag `arc-baseline` @ nwp `bb238a0` | `git -C ~/nwp checkout arc-baseline` |
| CP1 | 2026-07-24 | `local/browse` both versions preserved | `browse-original/{original-compact-from-ss,current-elaborated-from-ssc}/` + `ANALYSIS.md` | — | copy back from `browse-original/` |
| CP2 | 2026-07-24 | ssc loose custom plugins snapshot | `code-snapshots/ssc-custom-plugins-20260724.tar.gz` (sha `6640451d…`) | — | `tar -xzf … -C sites/ssc/dev` |
| CP-nwc | 2026-07-24 | nwc profile code (already safe) | — | `nwp/nwc.git` @ `ab5a40f` branch `deploy/2026-07-21-reconcile`, clean+pushed | `git -C sites/nwc/dev/html/profiles/custom/nwc checkout ab5a40f` |
| CP3 | 2026-07-24 | fresh remote DR snapshots — **verified** | `sites/ss/backups/ss-remote-20260724T180042.*` (74M tar+2.0M sql, sha256 OK) ; `sites/ssc/backups/ssc-remote-20260724T180130.*` (71M tar+2.6M sql, sha256 OK) | — | `pl restore <artifact>` (round-trip automated in P2) |

<!-- append checkpoints below as backups are taken -->
| CP11 | 2026-07-25 | nwd pre-cutover FULL live snapshot (ops#133 cutover) — **verified** | `sites/nwd/backups/nwd-remote-20260725T111425.tar.gz` (89M webroot) + `.sql.gz` (3.8M, sudo-mysqldump — live vendor had no drush) + `sites/nwd/backups/nwd-files-20260725.tar.gz` + `sites/nwd/backups/nwd-private-20260725.tar.gz`, all sha256 OK | — | untar webroot to `/var/www/nwd`, `sudo mysql nwd < dump`, untar files/private |
| CP12 | 2026-07-25 | nwd dev profile 37 uncommitted files (stale `develop`, 275 behind) | `sites/nwd/backups/nwd-dev-profile-pre-cutover-20260725.bundle` (verified, complete history; `.sha256` sidecar written 2026-07-26) + `sites/nwd/backups/nwd-dev-profile-pre-cutover-20260725.list` | local branch `safety/pre-cutover-20260725` in the pre-rebuild checkout (also in the bundle) | `git clone nwd-dev-profile-pre-cutover-20260725.bundle && git checkout safety/pre-cutover-20260725` |
| CP13 | 2026-07-25 | nwd dev DB+files local backup pre parity-rebuild | `sites/nwd/backups/20260725T112057-main-cc03f81b-pre-cutover-dev.{sql.gz,tar.gz,manifest.json}` | — | `pl restore nwd` from that artifact |

## Pre-arc site state (for reference)
- `~/nwp` (tool): branch `main` @ `bb238a0`
- `sites/nwc/dev`: branch `feat/structural-revision-gate`, 11 uncommitted (incl. untracked `html/` profile)
- `sites/ssc/dev`: branch `MOODLE_404_STABLE`, 10 uncommitted (all NWC Moodle plugins untracked)
- Latest pre-arc remote backups: nwc `nwc-remote-20260722T060724`, ssc `ssc-remote-20260722T060940`
| CP4 | 2026-07-24 | ops#68 prod2stg v2-resolve fix — **MERGED** (!139) | — | merge `b705710` on main | `git revert -m 1 b705710` |
| CP5 | 2026-07-24 | ops#124 backup prune (P2 retention, N1 gate) — **MERGED** (!140) | — | merge `627dba0` on main | `git revert -m 1 627dba0` |
| CP6 | 2026-07-24 | ops#127 restic --keep-within (DR ceiling, capability only) — **MERGED** (!141) | — | merge `bbd8ae6` on main | `git revert -m 1 bbd8ae6` (capability inert until `--keep-within` passed on ver) |
| CP7 | 2026-07-24 | ops#126 reversible retire of orphan /var/www/ss | box:/var/www/_retired_ss_20260724 (+ ss-remote-20260724T180042 sha OK) | — | `ssh gitlab@ss.nwpcode.org sudo mv /var/www/_retired_ss_20260724 /var/www/ss` |
| CP8 | 2026-07-24 | ops#127 (1/3) Drupal DR sanitiser --preserve-admin | — | `origin/ops-127` @ `b74e88d` (MR !142, worktree ~/nwp-ops127) | don't merge; `git branch -D ops-127` |
| CP8b | 2026-07-24 | ops#127 COMPLETE (3 parts) | — | `origin/ops-127` @ `cbb5cee` (MR !142) | don't merge; `git branch -D ops-127` |
| CP9 | 2026-07-24n | Plane1 tool-code batch (5 MRs SOLID) | — | branches ops-auto-* @ origin; MRs !143-!147 | don't merge; `git push origin --delete ops-auto-<x>` |
| CP10 | 2026-07-24n | P3 config-drift gate | — | origin/ops-63-config-drift @ 9eaf4c9 | don't merge; off-by-default if merged |
| CP14 | 2026-07-25 | nwd LIVE golden image (ops#133 demo tier) — **verified**, restores the site to its post-seed demo state | `sites/nwd/demo-golden-live/golden.db.sql.gz` + `sites/nwd/demo-golden-live/golden.files.tar.gz` + `sites/nwd/demo-golden-live/golden.manifest.json`; **integrity = the `.sha256` sidecars alongside each artifact** (the row deliberately no longer duplicates the digest — the golden is re-captured periodically and a copied digest goes stale silently; `pl rollback registry check` compares artifact↔sidecar) | — | `pl demo reset nwd --tier=live --force` (verifies both sha256 before and after upload) |
| CP15 | 2026-07-25 | nwd vhost — repo copy corrected php8.2→8.3 to match the box | `servers/nwpcode/nginx/conf.d/nwd.conf` @ branch `feat/demo-live-tier`; live copy unchanged throughout (box mtime Jul-24 21:49) | box `/etc/nginx/conf.d/nwd.conf` (untouched — **no .bak needed, no edit was made**) | `git checkout HEAD~1 -- servers/nwpcode/nginx/conf.d/nwd.conf` (repo-only; the box was never modified) |
| CP16 | 2026-07-26 | Art.9 pre-deploy remote snapshots (nwc + ssc) — **verified** | `sites/nwc/backups/nwc-remote-20260726T093907.{tar.gz,sql.gz}` (146M + 8.1M) ; `sites/ssc/backups/ssc-remote-20260726T094256.{tar.gz,sql.gz}` (74M + 2.6M) — all four sha256 OK, `.manifest.json` alongside | — | `pl restore <site>` from that artifact |
| CP17 | 2026-07-26 | ssc `mod/depthcontent` dev tree, pre-AMD-rebuild | `docs/reports/consolidation-arc-2026-07/code-snapshots/ssc-dev-depthcontent-prebuild-20260726.tar.gz` (sha `8d9e89d1…`) | — | `tar -xzf … -C sites/ssc/dev` |
| CP18 | 2026-07-26 | **live ssc `mod/depthcontent` immediately before the Art.9 gate deploy** (v2026072002, UNGATED) | box `/var/backups/ssc-depthcontent-pre-art9-20260726.tar.gz` (sha `65fff770…`, 74 KB) | — | `ssh gitlab@97.107.137.88 "sudo tar -xzf /var/backups/ssc-depthcontent-pre-art9-20260726.tar.gz -C /var/www/ssc"` then `sudo -u www-data php8.2 -d max_input_vars=5000 /var/www/ssc/admin/cli/upgrade.php --non-interactive` |
| CP19 | 2026-07-26 | item 2 recovery-path-repair — rollback dispatch table + state-dir ledger + `pl restore --remote` + registry checker | — | — | `git revert -m 1 <merge>`; legacy per-checkout `.rollback` dirs were never modified, so reverting loses no recovery point |
| CP20 | 2026-07-26 | leakage-gate un-blinded (fix-programme item 6) — config-only, no live site or server touched | — | branch `fix/leakage-gate-scope` off main; `.gitleaks.toml`, `.gitleaksignore`, `NOTICE`, `docs/CC0_DEDICATION.md`, `tests/unit/test-leakage-gate.bats` | `git revert -m 1 <merge>` — restores the previous (blind) gate exactly; no state migration, nothing deployed. To keep the rules but quieten CI, add fingerprints to `.gitleaksignore` (3-part form only) — **never** re-widen a `[allowlist] paths` entry. |
| CP-I4 | 2026-07-26 | Item 4 `pl-surfaces-report-reality` — oversight-surface fixes (no live site or server touched; the only behavioural change off the dev box is that the nightly `pl backup sweep` cron now exits 1 while any site has never been backed up) | — | branch `fix/pl-surfaces-report-reality` (MR pending) | `git revert -m 1 <merge>`; all changes are checks + new read-only verbs. Sweep exit code: revert restores exit 0. No state migration, no files written outside the repo. |
| CP21 | 2026-07-26 | **ci-gate-honesty** (item 5): 7 new `scripts/ci/*.sh` gates + 3 shrink-only baselines; `lint:bash`/`lint:yq-first`/`test:unit`/`test:integration`/`security:*`/`verify-signature` rewritten; `update-badges` + README shields deleted. Repo-only — **no site, server or live artefact touched**, so no data artifact exists or is needed. | — (no host state changed) | branch `ci-gate-honesty`, MR to `nwp/nwp` | `git revert -m 1 <merge-sha>` — restores the pre-fix `.gitlab-ci.yml`, README badges and `test-auth-logic.bats` skips. To relax ONE gate without reverting: set `allow_failure: true` on that job block. Baselines (`.yq-first-baseline`, `.security-meta-baseline`, `scripts/ci/.console-collect-baseline`) are inert once their scripts are gone. |
| CP22 | 2026-07-26 | **item 1 `secrets-registry-truth`** — `pl secrets` rewritten to check every declared `stored_in` location. Code-only; no live site, no server, no credential value touched. Registry migration is a *verb* (`pl secrets migrate-registry --apply`) that writes `private/secrets-registry.yml.bak` before it changes anything. | `private/secrets-registry.yml.bak` (created by the verb, beside the registry; the registry is gitignored so this is its only prior copy) | branch `ops-secrets-registry-truth`; the pre-fix script is recoverable at any time with `git show <merge-base>:scripts/commands/secrets.sh` | revert the MR (`git revert -m 1 <merge>`); to undo a registry migration: `cp private/secrets-registry.yml.bak private/secrets-registry.yml`. No state migration — `sync`/`adopt`/`discover-copies`/`verify-copy`/`capabilities`/`migrate-registry` are all NEW verbs, and `audit`/`lint`/`scan` only became *stricter*. |
| CP23 | 2026-07-26 | item 9 `moodle-ops-verbs` — `pl moodle cli` / `maintenance` / `core-patch` / `plugin drift`, `get_data_secret` depth fix, `doc-truth` raw-remote-cli rule. **No live site or server was modified**: every probe against the box (`ssc` plugin versions, `index.php`, `php -v`, `max_input_vars`) was a read-only grep. | — | nwp/nwp branch `fix/item9-moodle-ops-verbs` off main; nwp/ss-moodle-plugins branch `feat/core-patches-declared` (adds `core-patches/` only — disjoint from `auth/nwc/**` and `mod/depthcontent`) | `git revert -m 1 <merge>`. All new verbs default to dry-run, so reverting removes capability, never state. The only rewritten existing function is `get_data_secret`: keep the old 2-level parser for one release with `NWP_DATA_SECRET_LEGACY=1`. `moodle_write_config` now REFUSES an empty dbpass — if that blocks a promotion, provision `moodle.<site>.<tier>.db_password` rather than reverting (the previous behaviour wrote a broken config.php). The plugin-repo branch is unmerged; drop it with `git push origin --delete feat/core-patches-declared`. |
| CP-I8a | 2026-07-26 | item 8 art9-cross-repo-contract — Moodle `auth_nwc_set_consent` withdrawal endpoint rebased onto current ss-moodle-plugins main | pre-change ref = `nwp/ss-moodle-plugins` `origin/main` @ `95888f3` (merge of !7 `ops-137-depthcontent-amd-build`); pre-rebase branch tip preserved as `origin/fix/art9-withdrawal-moodle-failclosed` @ `af0f6f6` in the reflog of the scratch clone and in this row | branch `fix/art9-withdrawal-moodle-failclosed` @ `5c0c6bf` on `nwp/ss-moodle-plugins` | NOT DEPLOYED anywhere by this item — reverting is `git revert -m 1 <merge>` on ss-moodle-plugins, or simply not merging. The change is ADDITIVE (new WS function + `db/services.php` + `db/access.php`); the service ships `enabled: 0` + `restrictedusers: 1`, so merging alone changes no running behaviour until an operator enables it and issues a token |
| CP-I8b | 2026-07-26 | item 8 — `pairs/{ssc,ssd}.pair-contract.yml` v1→v2 (probe URLs + `crossref:` corpus) and `pl contracts crossref` | — | — | `git revert -m 1 <merge>` on nwp/nwp. Contract edits are pure YAML; `crossref` is a NEW verb with no existing caller, so a revert removes a check and restores nothing else |
| CP24 | 2026-07-26 | Item 3 `nested-repo-containment` — code-only. NO live site, server or nested site repo was touched; the changes are new files plus guards inside `nwp/nwp`. Backup-write behaviour changes (`pl backup` now fails closed into a publishable work tree) and `pl delete` refuses over dirty nested repos. | — | branch `fix/nested-repo-containment` @ `origin`, MR to `nwp/nwp` | `git revert -m 1 <merge>`. Escape hatches if a revert is not wanted: `NWP_ALLOW_BACKUP_IN_REPO=1` restores the old backup-write behaviour, `NWP_ALLOW_DELETE_DIRTY=1` restores the old delete behaviour. `.gitignore` blocks added by `pl site gitignore --fix` are delimited by `# >>> nwp containment … >>>` markers and can be deleted by hand; removing them cannot untrack anything, since ignore rules only ever affected untracked paths. |
| CP25 | 2026-07-26 | item 7 contract-and-boundary-gates — impact gate enforces adoption + scans lib/servers; boundary honesty reports CANNOT VERIFY; ops#143 inline fate manifest | — | — | `git revert -m 1 <merge>`; no live site, server or DB is written by this item, so reverting loses nothing but the gates |
| CP-I8c | 2026-07-27 | item 8 `vcs-truth` residuals — `pl snapshot` (bundle/verify/audit) + `lint:snapshot-bundles` CI gate, `check_vcs_safety` in `pl doctor` (`lib/vcs-truth.sh`), STALE-REF + pagination in `pl issue reconcile`. Code-only: **no live site, server, DB or credential was touched**; every git call in the new code is read-only (`rev-list`, `for-each-ref`, `show-ref`, `bundle verify` in a throwaway scratch repo). | **one tracked file was DELETED**: `docs/reports/consolidation-arc-2026-07/ssc-118-artifact/ops-118-moodle-art9-gate.bundle`, a *thin* bundle that restored nothing outside the authoring laptop. Its blob survives in history at `8e27949952be75ef73fe5ba7de3b48015c96af29`; the same commit content is in `ops-118-moodle-art9-gate.patch` beside it (retained) and on `nwp/ss-moodle-plugins` `origin/ops-137-depthcontent-amd-build`. | branch `fix/item8-vcs-truth-residuals` off main, MR to `nwp/nwp` | `git revert -m 1 <merge>` — restores the deleted bundle byte-for-byte along with everything else. To restore only the bundle: `git show 8e27949952be75ef73fe5ba7de3b48015c96af29 > docs/reports/consolidation-arc-2026-07/ssc-118-artifact/ops-118-moodle-art9-gate.bundle`. To keep the code but silence one gate: `pl snapshot audit` is only wired to the `lint:snapshot-bundles` CI job — set `allow_failure: true` on that block. `pl doctor`'s new section is a `check_*` function called from `main()`; deleting the two lines that call it restores the previous exit behaviour. |
| CP26 | 2026-07-26 | **repo↔box divergence, deliberate**: `servers/nwpcode/demo/nwd-demo-reset-restricted` gained an inline FATE MANIFEST ([G9], ops#143) in the REPO ONLY. The box copy at `/usr/local/bin/nwd-demo-reset-restricted` on git.nwpcode.org is unchanged and keeps working exactly as before (the change is additive reporting ahead of the destructive section). It goes live at the next `bash servers/nwpcode/demo/install-box.sh` — a server-config write on the GitLab box, which is item 10 / operator territory, NOT done here | box copy untouched; no backup needed because nothing was modified on the box | repo copy on branch `fix/item7-contract-and-boundary-gates` | to align: `bash servers/nwpcode/demo/install-box.sh` (installs the wrapper; verify with the `dry-run` action word, which exits before anything destructive). To reverse the repo change: `git revert -m 1 <merge>` |
| CP-B7 | 2026-07-26 | **B7 stale-branch sweep** — deleted 3 heads from `origin` (`pl-rollback-stdin-fix` `1813994`, `stg2live-drush-graceful` `3f73691`, `chore/gitleaks-allowlist-issue-urls` `0b3f644`) and 2 duplicate heads from the `archive` remote (`pl-rollback-stdin-fix` `1813994`, `stg2live-drush-graceful` `3f73691`). No live site, server, DB or file outside GitLab ref storage was touched; MRs !10/!11/!50 were already `closed` and **no open MR** used any of them as source or target (checked via API before the push). | — (no data artifact; the commits themselves are the artifact) | GitLab preserves all three commits permanently at `refs/merge-requests/{10,11,50}/head` on project 9 — verified by `git ls-remote origin 'refs/merge-requests/N/head'` returning the exact SHAs before deletion. They are also reachable in any clone that fetched them. | Restore any branch with `git push origin <sha>:refs/heads/<name>`, fetching the sha first if needed: `git fetch origin refs/merge-requests/10/head` → `1813994`, `/11/head` → `3f73691`, `/50/head` → `0b3f644`. Archive twins: `git push archive <sha>:refs/heads/<name>`. **Do not restore `chore/gitleaks-allowlist-issue-urls` in order to merge it** — it is a net revert of `.gitleaks.toml` (see decision-log `b7-gitleaks-branch-is-a-net-revert-not-a-fix`); restore it only as a historical read. |
| CP-B2 | 2026-07-26 | **`origin/ops-127` DELETED** (supersedes CP8/CP8b, which said "don't merge; `git branch -D ops-127`"). No live site, server, DB or artifact touched — a forge ref deletion only. The work itself is on main via `ops-127-recovery` (`089c4ca`) and its two bats suites are 12/12 green there. | — (no data artifact; nothing was deployed) | deleted ref `refs/heads/ops-127` @ `cbb5cee`; preserved server-side at `refs/merge-requests/149/head` (`cbb5cee`) and `refs/merge-requests/142/head` (`b74e88d`); local branch ref `ops-127` in `/home/rob/nwp` also survives | `git fetch origin refs/merge-requests/149/head:ops-127 && git push origin ops-127` restores it byte-for-byte. **But do not merge it if you do** — it lacks main's `files_secrets_verify` pre-snapshot check + restic secret `--exclude`s, and re-adds the retired `scripts/f26/moodle/auth_nwc/` sources at `2026071101/1.0.0` while live ssc runs `2026072400/1.2.0-draft` (Art.9 consent-gate downgrade). |
| CP27 | 2026-07-26 | item 2 oversight-honesty — UNKNOWN/UNSCANNED states in `pl todo`/`pl rag`, Moodle parser fix, backup-integrity helper, pause visibility, `pl notify` | — | branch `ops-auto/item2-oversight-honesty`, MR TBD | `git revert -m 1 <merge>`. **Repo-only: no live site, no server and no host state was modified.** The only state written outside the repo is `private/rag/state.json` and `private/update-awareness/*.json`, both fully regenerated by one `pl audit --all` / `pl rag`. Reverting restores the previous (over-optimistic) grades; it does not lose data. |
| CP-I5 | 2026-07-26 | **item 5 `art9-gdpr-cross-repo` (tail)** — new `pl erasure` (plan/verify/status/list/execute), new `pl pair reconcile`, new `pl contracts guards`; `erasure:` + `guards:` blocks added to `pairs/ssc.pair-contract.yml`. **Repo-only: no live site, no server, no member record was read or written.** The only files created outside the repo are erasure *request* records under `private/erasure/` (gitignored), which hold a uuid and no PII. | — (no host state changed; `private/erasure/` is regenerable and holds no PII) | branch `fix/item5-art9-erasure-verbs` off main @ `c071b88` | `git revert -m 1 <merge-sha>`. Every new verb is read-only or dry-run-by-default, so a revert removes capability and restores no state. `pl erasure execute` never ran (it fails closed with `CHANNEL-NOT-DEPLOYED` on the real estate — ops#81 P1/P2 are unbuilt). `pl pair reconcile --apply` never ran (it needs an explicit `--repair-cmd`, which was only ever a fixture `echo`). To keep the verbs but silence the new ops#138 red, delete the `guards:` block from the contract — do **not** add a fake call site. |
| CP-I3 | 2026-07-26 | Item 3 `nested-repo-containment` — code-only. NO live site, server or nested site repo was touched; the changes are new files plus guards inside `nwp/nwp`. Backup-write behaviour changes (`pl backup` now fails closed into a publishable work tree) and `pl delete` refuses over dirty nested repos. | — | branch `fix/nested-repo-containment` @ `origin`, MR to `nwp/nwp` | `git revert -m 1 <merge>`. Escape hatches if a revert is not wanted: `NWP_ALLOW_BACKUP_IN_REPO=1` restores the old backup-write behaviour, `NWP_ALLOW_DELETE_DIRTY=1` restores the old delete behaviour. `.gitignore` blocks added by `pl site gitignore --fix` are delimited by `# >>> nwp containment … >>>` markers and can be deleted by hand; removing them cannot untrack anything, since ignore rules only ever affected untracked paths. |
| CP-I3b | 2026-07-26 | item 3 follow-up — (a) repo: `pl site gitignore --exposed\|--all` + `containment_check_tracked_*` (the already-published half) and the S18 vacuous-step fix; (b) **fleet**: the containment block was back-applied to the 6 nested repos that held an un-ignored credential or backup payload on disk beside a live remote — `sites/avc/{backups,dev,stg}`, `sites/nwc/{dev,stg}`, `sites/nwd/dev`. Only each repo's `.gitignore` was modified. NO live site, server, database or tracked file was touched, and no history was rewritten. | pre-change `.gitignore` of all 6 repos saved to `scratchpad/i3-gitignore-backup/<site>_<env>.gitignore` (`avc_backups`, `avc_dev`, `avc_stg`, `nwc_dev`, `nwc_stg`, `nwd_dev`); each is also recoverable in-repo with `git -C sites/<s>/<env> checkout -- .gitignore`, since the file was tracked and clean before | nwp/nwp branch `fix/nested-repo-containment` (MR !175, rebased onto current main) | Repo half: `git revert -m 1 <merge>`. Fleet half: `git -C sites/<s>/<env> checkout -- .gitignore` per repo, or delete the block between the `# >>> nwp containment … >>>` / `# <<< nwp containment <<<` markers by hand. Reversing loses no containment that existed before, because an ignore rule only ever affected paths that were untracked to begin with — it cannot untrack, and could not have removed anything. Re-running `pl site gitignore --fix` is idempotent and re-applies it. |
| CP-I6 | 2026-07-26 | item 6 `pl-host` — host-state verbs (`pl host capture/diff/apply/schedule`, `pl server health`, `pl server forge status`, `pl logs`, `pl loop --host`, `pl schedule host/where`), `servers/` gitignore allowlist, `lib/safe-ops.sh` deleted | — | branch `fix/item6-pl-host` off main; new `lib/host-capture.sh`, `scripts/commands/logs.sh`, `tests/unit/test-host.bats`, `tests/fixtures/dead-libs.allow` | `git revert -m 1 <merge>`. **No server was modified** — every probe in this item is read-only (`crontab -l`, `cat /etc/…`, `dpkg-query`, three `/proc` reads, `df`); `--execute` is not enabled on `host apply`/`host schedule`. Reverting restores the blanket `servers/*` ignore and re-adds the dead `lib/safe-ops.sh`; nothing else changes state. To keep the verbs but disable the swap-pressure grading, set `NWP_HEALTH_MIN_SWAP_FREE_PCT=0` (no code change). |
| CP-I7 | 2026-07-26 | **item 7 `host-state-capture`** — load-bearing host state pulled off the boxes into `servers/{met,nwpcode}/system/**` via the new read-only `pl server-state`, plus a declared (NOT YET APPLIED) `servers/nwpcode/php/conf.d/90-nwp-moodle.ini`. **Nothing was written to any host**: every artifact was fetched with `cat` / `systemctl cat` / `ufw status` over ssh with `BatchMode=yes` and a 30 s timeout. The one behavioural change to an existing verb is that `pl rollback registry check` now also asks whether each artifact is durable, and so exits 1 on 6 pre-existing rows. | — (no host state changed; the captured files are new repo copies of what already runs) | branch `fix/item7-host-state-capture` off main | `git revert -m 1 <merge>`. Reverting removes the versioned copies and the verb; it cannot change any host, because no host was changed. The captured files are regenerable at any time with `pl server-state capture <host>`. If the stricter registry check is unwanted before the 6 LAPTOP-ONLY rows are resolved, revert only the `_artifact_is_tracked` block in `scripts/commands/rollback.sh` — the rest of the item is independent of it. |
| CP-B3 | 2026-07-26 | B3 branch-janitor `ops-93` — **nothing was deleted**. Repo-only: one new operator-run script in nwp/nwp, plus a test-only branch pushed to `nwp/ss-moodle-plugins`. No live site, server, DB or deployed plugin touched (project 33 was read via a throwaway shallow clone). | — (no host state changed; `refs/heads/ops-93` and `/home/rob/nwp-ops93` are intact and ARE the backup of the rescued test) | nwp/nwp branch `rescue/ops-93-guild-cohort-coverage`; nwp/ss-moodle-plugins branch `rescue/ops-93-guild-cohort-logic-test` @ MR !10 (unmerged) | `git revert -m 1 <merge>` on nwp/nwp (removes a check, restores nothing else). On the plugin repo: `git push origin --delete rescue/ops-93-guild-cohort-logic-test`, or just don't merge !10 — the commit is one new test file plus its registration, additive and inert until `tests/run-standalone.sh` is run. |
| CP-I9 | 2026-07-26 | item 9 `docs-pl-first` — `pl doc-truth` gains a `dead-command-ref` class + whole-repo markdown scope; 7 guides/runbooks burned down off five deleted root scripts and the nonexistent `pl deploy`; 7 raw `print_error` recovery strings in `stg2live`/`live2prod`/`stg2prod` replaced with `pl drush` / `pl rollback`. **Repo-only: no live site, no server, no host state was touched — the only host contact was `./pl commands` on this workstation.** | — | branch `ops-auto/item9-docs-pl-first`, MR to `nwp/nwp` | `git revert -m 1 <merge>`. Nothing is deployed and no state is written outside the repo, so reverting loses only the gate. To keep the docs but quieten the gate without reverting: `pl doc-truth --baseline` re-accepts current drift (then re-add the header comment, which regeneration drops). The recovery-string change is text-only — the *code path* around it is untouched, so a revert restores the old printed instruction and nothing else. `.doc-truth-baseline` is SHRINK-ONLY: 395 rows today (264 dead-command-ref · 102 dead-link · 29 raw-remote-cli); adding a row is a decision to be recorded in the decision log. |
| CP-20260727-pairguard | 2026-07-27 | **pair_guard binds the real ssc↔nwc pair** — repo-only. NO live site, server, DB or `private/pairs/` state was touched; the change is `lib/pair.sh` membership resolution + one new bats file + docs, and two gitignored operator files (`sites/ssc/.nwp.yml`, `sites/ssd/.nwp.yml` pairing key). Net effect is STRICTLY MORE REFUSALS: a full-DB `pl stg2live nwc\|ssc --tier=live` is now blocked where it was allowed. | branch `fix/pair-guard-binds-real-pair` off `origin/main` `aec4adc` | `git revert -m 1 <merge>` in nwp/nwp. Operator config: restore the map form in `sites/ss{c,d}/.nwp.yml` and drop `sites.ssc.paired_with` from `nwp.yml` (both gitignored; see the decision-log entry for the exact prior text). To un-block a deploy WITHOUT reverting: `NWP_PAIR_GATE_SOFT=true` (audited, ledgered) or `--override-pair` for the D6 invariant itself. |
| CP-20260727-failopen | 2026-07-27 | **four guards fail closed on an unreadable input** — `lib/canonical.sh` (canonical phase + maturity class → `cannot-verify`), `lib/sanitizers/mayo.sh` + `lib/sanitizers/standard.sh` (PII sweep `gzip -t` + non-empty-stream assert), `lib/moodle-deploy.sh` + `scripts/commands/moodle.sh` (core-patch declaration reader is tri-state). Repo-only: **no live site, server, DB, `private/` state or operator config was touched**, and `nwp.yml` was neither staged nor modified. Net effect is STRICTLY MORE REFUSALS on inputs that could not be read; behaviour is unchanged whenever the config parses. | — (no host state changed) | branch `fix/guards-failopen-cannot-verify` off `origin/main` `fa4c10d` | `git revert -m 1 <merge>` in nwp/nwp. To un-block a deploy WITHOUT reverting: `NWP_CANONICAL_GATE_SOFT=true` (warns + writes `private/canonical/<site>.log`). The sanitizer and core-patch refusals have **no** override by design — fix the artifact or the declaration. Reverting removes checks only; nothing is deployed and no state is written outside the repo, so a revert loses no recovery point. |
| CP-20260727-pubrelfailopen | 2026-07-27 | **public-release scrub gate made fail-closed** — `pubrel_scan` discarded gitleaks' exit status and decoded a missing/unparseable report as "0 findings = clean"; `PUBREL_DOMAIN_RE` covered 4 of the gate's 6 identity rules. Test-and-docs only: **no live site, server, DB or credential was touched, and `.gitleaks.toml` was NOT modified** (it was altered only inside a throwaway reproduction and restored in the same step — verified by `git status`). Net effect is STRICTLY MORE REFUSALS: a scan that cannot run now fails instead of reporting clean. | — (no host state changed) | branch `docs/pubrel-genericise-operator-identifiers` off `origin/main` `9112070` (MR !197) | `git revert -m 1 <merge>` in nwp/nwp — restores the previous fail-open helper and the 8-test suite exactly; nothing is deployed and no state is written outside the repo. To keep the fix but stop the suite failing while a genuinely broken `.gitleaks.toml` is repaired, fix the config — do **not** re-widen the `except Exception` or drop the rc check. The 75 pruned `.gitleaksignore` fingerprints are restorable from `git show <merge-base>:.gitleaksignore`. |
| CP-20260727T1105-mainred | 2026-07-27 | **`main` unblocked: brick bundle replaced by a standalone patch + one doc-truth drift item.** Repo-only: **no live site, server, DB or credential was touched.** Host contact was read-only — a shallow clone of `nwp/ss-moodle-plugins@gdpr/art9-depthcontent-fixes` and two GitLab API commit lookups, used to PROVE the deleted artifact's content is preserved (40 files, zero diff) before deleting it. **One tracked file was DELETED**: `docs/reports/consolidation-arc-2026-07/gdpr-artifacts/ssc-depthcontent-art9-20260726.bundle`, an undeclared thin bundle whose prerequisite `346025ce…` is 404 in the canonical repo and exists only in `sites/ssc/dev` on this laptop — i.e. it restored nothing anywhere else. | replaced in-place by `ssc-depthcontent-art9-20260726.patch` (`git show bcc7f496`, 286 KB, standalone) beside a `README.md` recording the verification; the bundle blob also survives in history at `e82c87340a090761a0ca26f5db53ca868772c846`; content additionally preserved on `nwp/ss-moodle-plugins` `gdpr/art9-depthcontent-fixes` | branch `fix/main-red-doctruth-and-brick` off `origin/main` `084b679` | `git revert -m 1 <merge>` — restores the bundle byte-for-byte and the prior doc line. To restore only the bundle: `git show e82c87340a090761a0ca26f5db53ca868772c846 > docs/reports/consolidation-arc-2026-07/gdpr-artifacts/ssc-depthcontent-art9-20260726.bundle` — but note it will re-red `lint:snapshot-bundles`, because it genuinely cannot restore outside `sites/ssc/dev` on this laptop. |
| CP-20260727-ops146a | 2026-07-27 | **ops#146 pre-change LIVE DB snapshots (nwd + ssd)** — sha256-verified on pull. DB-only by deliberate choice: every nwd change in this item is DB-resident (simple_oauth config, 3 oauth2_scope entities, the `ssd_moodle` consumer, the `grant simple_oauth codes` permission, one menu link) plus a brand-new `/var/www/nwd/keys/` directory that overwrites nothing; the nwd webroot baseline is CP28 (2026-07-26T14:07, full tar) and was re-verified unchanged. A full webroot pull was started and abandoned at ~17% — it was running at ~0.8 MB/min (≈110 min) and would have saturated the link the deploy needed. | `sites/nwd/backups/nwd-remote-20260727T012627.sql.gz` (+`.sha256`, +`.manifest.json`) ; `sites/ssd/backups/ssd-remote-20260727T013313.sql.gz` (+`.sha256`, +`.manifest.json`) | — | `pl restore <site>` from that artifact. For nwd webroot: CP28. |
| CP-20260727-ops146b | 2026-07-27 | **ssd LIVE first-install of 5 plugins** (`auth/nwc`, `mod/depthcontent`, `local/browse`, `local/mentor`, `local/practice`) from `nwp/ss-moodle-plugins@main` (6fc400a). All five were ABSENT on live beforehand, so the change is purely additive — there is no prior version to restore. | box `/home/gitlab/nwp-snapshot-ssd-moodledb-*.sql.gz` (pre-deploy DB) + `nwp-snapshot-ssd-plugins-*.tar.gz` (empty tar — correct: nothing existed to snapshot) ; local rollback entry `~/.local/state/nwp/rollback/ssd_live_*.json` | — | `pl moodle rollback ssd --tier=live execute` restores the DB. To remove the code: `ssh gitlab@97.107.137.88 'sudo rm -rf /var/www/ssd/{auth/nwc,mod/depthcontent,local/browse,local/mentor,local/practice}'` then `pl moodle cli ssd --tier=live --execute -- admin/cli/upgrade.php --non-interactive`. |
| CP-20260727-ops146c | 2026-07-27 | **nwd LIVE golden RE-CAPTURED to include the OIDC wiring** (supersedes CP30). This is the load-bearing safety row: the CP30 image predates every ops#146 change, so leaving it in place would have had the 01:00–03:30 cron silently un-wire the issuer on the next run. Staged to the box and sha-verified there. The prior (CP30) image is preserved verbatim. | `sites/nwd/demo-golden-live/` — db sha `5e6fc928393c…`, files sha `2af0b50e2038…`, captured `2026-07-26T15:52:56Z`; staged to box `/var/lib/nwp-demo/nwd/golden/` (`sha256sum -c` OK there). Prior image kept at `sites/nwd/backups/golden-live-pre-ops146-*/` | — | copy `sites/nwd/backups/golden-live-pre-ops146-*/` back over `sites/nwd/demo-golden-live/` and re-stage the 5 files to `/var/lib/nwp-demo/nwd/golden/`. **Only as a last resort:** the CP30 image restores an nwd with NO issuer keys, NO `ssd_moodle` consumer and no scopes — i.e. it re-breaks nwd↔ssd SSO. |
| CP-20260727-ops146d | 2026-07-27 | **Local host state touched (dev workstation, all reversed or recorded)**: (1) the `nwd_demo_reset` cron line was commented out for ~90 min while the golden was stale, then restored and `diff`-verified byte-identical to the pre-session copy; (2) `sites/ssd/.nwp.yml` `plugins_ref` `gdpr/art9-depthcontent-fixes`→`main` and a new `cli_php_version: "8.3"`; (3) `nwp.yml` gained `sites.ssd.paired_with: nwd`, which ARMS the previously-inert ADR-0031 pair guard for the demo pair only. | `private/demo/crontab.pre-ops146.bak` ; `private/demo/ssd.nwp.yml.pre-ops146.bak` ; `private/demo/nwp.yml.pre-ops146.bak` | — | `crontab private/demo/crontab.pre-ops146.bak` ; `cp private/demo/ssd.nwp.yml.pre-ops146.bak sites/ssd/.nwp.yml` ; `cp private/demo/nwp.yml.pre-ops146.bak nwp.yml`. Disarming the pair guard again is just removing `sites.ssd.paired_with`. |
| CP-I1 | 2026-07-26 | **item 1 `secrets-truth-and-gate`** — NO-PROBE / TIER / UNTRACKED-REGISTRY lint rules, `rotate` stamp gate, audit retry + `last_successful_audit`, `pl secrets probe-scaffold\|registry-track\|cron`, `lint:secrets` CI job. **Repo-only: no live site, no server, no credential value and no crontab was written.** The one read against the estate was `pl secrets lint` (local file reads only, no network). | byte copy of the live registry taken BEFORE any work, at `<scratch>/item1/registry-BEFORE.yml` (sha256 recorded in the decision log). The live `~/nwp/private/secrets-registry.yml` was **not modified by this item**. | branch `fix/item1-secrets-truth-and-gate` off `origin/main` @ `c071b88` | `git revert -m 1 <merge>`. Every change is a NEW verb or a STRICTER check, so a revert removes capability and restores no state. To keep the code but quieten one finding: `lint` rules are individually greppable (`NO-PROBE`, `TIER:`, `UNTRACKED-REGISTRY`); to relax the CI gate without reverting set `allow_failure: true` on the `lint:secrets` block. `rotate`'s new stamp gate has the same escape hatch `done` already had: `NWP_SECRETS_FORCE_DONE=1`. |
| CP-20260727-secretstruth | 2026-07-27 | `private/secrets-registry.yml` (untracked source of record) | pre-change copy at `scratchpad/cp/secrets-registry.yml.CP-20260727-secretstruth`, sha256 `367e9a4a1b5d1115…`; `pl secrets migrate-registry --apply` also wrote `private/secrets-registry.yml.bak` | restore either file over the registry; no tracked file depends on it, so no code rollback is needed | item 1 `secrets-truth-and-gate` — scope correction, provision-token entry, grammar migration, fleet-copy declarations |
| CP-20260802-onb1 | 2026-08-02 | overnight-B nwd LIVE code deploy (stg refreshed to nwp/nwc main b13adf0, stg2live --code-only --override-pair; hooks nwc_core_10111/nwc_demo_10001/nwc_editorial_10010 ran) + identity scrub (approver_email→apply@, data_policy rev 2 deleted, config_export/import copies refreshed) + ops#169 private PNG seeded | `/home/gitlab/nwp-snapshot-nwd-webroot-20260802-015007.tar.gz` (off-host, sha `19c1e79d…`) | — | box: gunzip -c ~/nwp-snapshot-nwd-dbs-20260802-015007.sql.gz | sudo mysql (sha d75d974c…) + untar webroot snapshot; or pl demo reset nwd (pre-overnight golden below) |
| CP-20260802-onb2 | 2026-08-02 | overnight-B ssd LIVE: site identity (shortname 'ssd'→'Saint School (Demo)' via posture MR !294), Moodle cron installed (www-data crontab, php8.2, */15 — ssc precedent; first run OK), depthcontent plugin 2026072600→2026080101 + restore subplugin (ss-moodle-plugins MR !17), seeder self-repair (SEED-OK courses=3), 55-course import (58 courses/250 depthcontent; one purge pass of 54 stripped rows, MR !299) | `/home/rob/nwp/sites/ssd/backups/ssd-remote-20260801T212351.sql.gz` (off-host, sha `0ceb2fce…`) | — | pl restore from that artifact, or box pre-deploy nwp-snapshot-ssd-moodledb-20260802-023131.sql.gz; cron rollback = remove ssd line from www-data crontab (artifact servers/live/system/moodle-cron-scheduler) |
| CP-20260802-onb3 | 2026-08-02 | overnight-B PAIRED golden recapture (nwd 3.8M/37K + ssd 2.4M/11M, captured back-to-back, staged to box sha-verified). Supersedes the 2026-08-01 pair (nwd 10:42:45Z, ssd 11:50:33Z). Curated items re-verified: 30 codes (registry=site=staged), zero operator identifiers in BOTH goldens (whole-DB sweeps), issuer 'Narrow Way Commons', banner row, sessions/logstore structure-only | `/home/rob/nwp/sites/nwd/demo-golden-live.pre-overnight-20260802/golden.db.sql.gz` (off-host, sha `4081187f…`) | — | cp -a demo-golden-live.pre-overnight-20260802 over demo-golden-live (both sites; ssd pre sha cda1a5be71aade9cc5867cb167068bfbc0c9e35c783b2aec1929d8c7184dddee) and re-stage with install-box.sh — NOTE: pre-overnight goldens predate ALL of tonight's fixes |
| CP-20260802-feedbacksync | 2026-08-02 | **ops#161 pre-wipe tester-feedback sync, interlocked with ops#140** — `demo_feedback_sync` in `lib/demo.sh` (fail-OPEN, always returns 0), one hook in each of the three reset paths in `scripts/commands/demo.sh`, a new `pl demo feedback-sync` verb, a token-free pending-count WARN in `servers/live/demo/nwd-demo-reset-restricted`, 20 bats tests, docs. **Repo-only: no live site, server, DB, credential, crontab or `private/` state was touched, and the box wrapper was NOT re-installed** — the edited file is the versioned copy, the deployed one at `/usr/local/bin/nwd-demo-reset-restricted` is unchanged until someone runs `install-box.sh`. Net effect while unmerged: none. Net effect once merged AND deployed: a reset additionally pushes pending feedback, and refuses to push from a site whose payload is not provably minimised. | — (no host state changed) | branch `fix/ops-161-feedback-sync-minimised` off `origin/main`; companion `nwp/nwc` branch `ops-161-pin-minimisation-marker` (test-only) | `git revert -m 1 <merge>` in nwp/nwp — removes a capability and a warning, restores no state. To disable the sync WITHOUT reverting: remove `gitlab.feedback_sync_token` / `gitlab.api_token` from `.secrets.yml` on the host that runs the reset, and every hook logs `feedback-sync-skipped` and proceeds. To disable only the ops#140 interlock: there is deliberately **no** override — a payload that is not provably minimised is never sent. The nwc companion is a test file only; not merging it changes no runtime behaviour, it only leaves the interlock's marker method unpinned. |
| CP-20260802-yqfirst | 2026-08-02 | **ops#196 D9 `lint:yq-first` correctness** — the scanner's variable learning rescoped from FILE-wide to bash's real scoping plus assignment reachability, single-line awk arguments read after the closing quote (not the whole `awk …` text), function attribution given an indentation-keyed stack, heredoc bodies excluded; `.yq-first-baseline` RE-DERIVED 122 → 129 rows (0 removed, 7 added — all pre-existing parsers the old heuristic could not see). **Repo-only: no live site, server, DB, credential or `private/` state was touched, and `.gitlab-ci.yml` was NOT modified.** Net effect on the gate is one class fewer false positive and seven more genuine offenders recorded. | pre-change baseline is `git show <merge-base>:.yq-first-baseline` (122 rows); the audit that classified all 129 rows is in the MR description and the ops#196 note | branch `fix/ci-gate-correctness` off `origin/main` `326cb91` | `git revert -m 1 <merge>` in nwp/nwp — restores the old scanner and the 122-row baseline together (they must move as a pair; the 122-row baseline against the new scanner would report 7 NEW offenders and red the gate, and the 129-row baseline against the old scanner would report 7 STALE entries). To keep the scanner but drop the 7 new rows, convert those functions to yq and run `scripts/ci/lint-yq-first.sh --update-baseline` — the `#=` provenance block survives regeneration. |
| CP-20260802-boundaryclassify | 2026-08-02 | **ops#196 D11 `boundary:classify` safe-to-block** — `boundary_contract_readable` now PARSES the pair contract and requires ≥1 declared surface (a corrupt or `boundary: {}` contract previously returned `INTERNAL`/`uncomputable:false`/exit 0, i.e. fail-OPEN under a blocking gate); new `boundary_unreadable_why` names which of the four causes fired; `scripts/ci/ensure-yq.sh` made concurrency-safe (flock + download-to-temp + atomic `mv`, no more `rm -rf` of a live cache), network-resilient (bounded retries, hard timeout, a runner-side diagnostic instead of `curl: (7)`), and non-silent (post-install exec check; the flock wrapper propagates the child's status). **Repo-only: no live site, server, DB or credential was touched, and `.gitlab-ci.yml` was NOT modified** — the blocking flip from !295 is left exactly as the operator ratified it. Net effect is STRICTLY MORE REFUSALS on inputs that could not be read. | — (no host state changed) | branch `fix/ci-gate-correctness` off `origin/main` `326cb91` | `git revert -m 1 <merge>` in nwp/nwp — removes checks and restores no state. To make `boundary:classify` non-blocking WITHOUT reverting the correctness fixes, set `allow_failure: true` on that job in `.gitlab-ci.yml` (operator territory) or drop `--fail-uncomputable` from its two script lines; the classifier itself still exits 0 for both INTERNAL and BOUNDARY-TOUCHING, so that change only stops "this runner could not classify" from being red. |
| CP-D17 | 2026-08-02 | **D17 pair-cut monitoring + ops#198 demo-status truth** — new `check_demo_pair_cut` in `pl todo`/`pl rag` (filesystem-only, no ssh, fits the 45s sweep budget) and `pl demo status --tier=live` now reads the BOX's own reset record over the existing read-only restricted-key `status` action word. **No live writes**: the box was only ever asked `status`, which reads. No site, server or golden touched. | — (no host state changed) | branch `feat/pair-cut-monitor-and-demo-status-truth`, MR to `nwp/nwp` | `git revert -m 1 <merge-sha>`. To silence only the new check without reverting: set `settings.todo.categories.demo_pair_cut: false` in `nwp.yml` (defaults true when absent). To skip only the box probe: `DEMO_STATUS_NO_BOX=1 pl demo status <site> --tier=live`. No state migration — the check is read-only and `cmd_status_box` is a new function; the pre-fix behaviour is recoverable at any time with `git show <merge-base>:scripts/commands/demo.sh`. |
| CP-20260802-ops170 | 2026-08-02 | **ops#170 paired capture/reset at `--tier=live`** — the two `demo_is_live` refusals in `cmd_golden_paired` / `cmd_reset_paired` replaced by a built live path (`cmd_reset_paired_live`), plus a root fix to `demo_live_ctx`'s process-global memo. **Repo-only: NOTHING was run against live. No site, server, DB, golden image, crontab or box wrapper was touched** — the paired live path has never been executed against the estate. Net behaviour change for an EXISTING command: `pl demo reset nwd\|ssd --tier=live` now auto-upgrades to the PAIRED path (announced on stdout); `--no-pair` restores the single-host behaviour exactly, and the box-resident nightly wrappers (`servers/live/demo/*-demo-reset-restricted`, which drive the armed cron from met) are UNCHANGED and unaffected. | — (no host state changed; the live goldens in `sites/{nwd,ssd}/demo-golden-live/` were not read or written) | branch `fix/ops-170-paired-live-erasure` off `origin/main` `e10a5f8` | `git revert -m 1 <merge-sha>` in nwp/nwp — restores both refusals and the one-shot live context. Nothing is deployed, so a revert loses no recovery point. To keep the code but stop the auto-upgrade for one run: `--no-pair`. To keep the paired path but skip the box-lock gate: there is deliberately NO env override — the gate exists so a workstation run cannot collide with the 01:00–03:30 nightly; run outside that window instead. A `pair.cut.json` written by a paired live capture is inert on its own: without it a paired reset simply refuses. |
| CP-20260802-ops170b | 2026-08-02 | **ops#170 paired capture/reset at `--tier=live`** — the two `demo_is_live` refusals in `cmd_golden_paired` / `cmd_reset_paired` replaced by a built live path (`cmd_reset_paired_live`), plus a root fix to `demo_live_ctx`'s process-global memo. **Repo-only: NOTHING was run against live. No site, server, DB, golden image, crontab or box wrapper was touched** — the paired live path has never been executed against the estate. Net behaviour change for an EXISTING command: at `--tier=live` the paired path is **opt-IN** (`--with-pair`); a bare `pl demo reset nwd\|ssd --tier=live` on a coupled half now **REFUSES** and names the flag instead of silently wiping one host (`--no-pair` restores the previous single-host behaviour exactly, and a bare live `pl demo golden` is unchanged). The box-resident wrappers (`servers/live/demo/*-demo-reset-restricted`, which drive the armed cron from met) are UNCHANGED and unaffected. | — (no host state changed; the live goldens in `sites/{nwd,ssd}/demo-golden-live/` were not read or written) | branch `fix/ops-170-paired-live-erasure` off `origin/main` `e10a5f8` | `git revert -m 1 <merge-sha>` in nwp/nwp — restores both refusals and the one-shot live context. Nothing is deployed, so a revert loses no recovery point. To un-refuse a bare live reset of a coupled half without reverting: `--no-pair` (loud + logged, as before). To keep the paired path but skip the box-lock gate: there is deliberately NO env override — the gate exists so a workstation run cannot collide with the 01:00–03:30 nightly; run outside that window instead. A `pair.cut.json` written by a paired live capture is inert on its own: without it a paired reset simply refuses. |
| CP-20260802-phpfloor-verb | 2026-08-02 | **`pl host apply --kind=php --execute` — the max_input_vars floor becomes a verb (operator ruling D10).** New `lib/php-floor.sh`; `--execute` enabled for `--kind=php` ONLY (every other kind stays declare-only, CP-I6 unchanged); `declared_ini:` added to `php_floors` in `servers/{live,nwpcode}/system/inventory.yml`; new `servers/live/php/conf.d/90-nwp-moodle.ini`; `impact_confirm` gains an optional 4th arg (warning override) — default behaviour of every existing caller is byte-identical; new `tests/unit/test-php-floor.bats` (18 cases, 14 of them RED on `origin/main` before this branch). | — (code + declaration only; the live-box write is CP-20260802-phpfloor-live below) | branch `feat/host-apply-php-floors` off `origin/main` `326cb91` | `git revert -m 1 <merge>`. Reverting removes the capability and restores the "not enabled in this release" refusal; it does NOT undo the live-box write (see the next row). To keep the verb but forbid execution without reverting, delete the `php_selected` branch in `cmd_apply` — the declare-only path below it is untouched. |
| CP-20260802-phpfloor-live | 2026-08-02 | **live box: `90-nwp-moodle.ini` brought under the verb's management on both 8.2 SAPIs.** The floor was ALREADY in force (hand-placed by `scp`+`sudo cp` 2026-08-01, which is what ruling D10 is about); this run replaced those bytes with the reviewed `servers/live/php/conf.d/90-nwp-moodle.ini` (`sha256 b7609d805e1a2da9…`), reloaded `php8.2-fpm` once, and re-measured `max_input_vars=5000` on `8.2/fpm` and `8.2/cli`. The VALUE did not change — 5000 before, 5000 after — so no site behaviour changed. Preflight `pl server health live`: 2221 MB free. Post-check: `ssc` 200, `ssd` 200. | box `live:/var/backups/nwp-php-floor/90-nwp-moodle.ini.8.2-fpm.20260801T193142Z.bak` and `…8.2-cli.20260801T193142Z.bak`, both `sha256 75b38a11be215dd0…` (the hand-placed bytes, verbatim) — taken by the verb before each write | — | Restore the previous (hand-placed) bytes: `pl drush`-free, box-side — `sudo install -m0644 /var/backups/nwp-php-floor/90-nwp-moodle.ini.8.2-fpm.20260801T193142Z.bak /etc/php/8.2/fpm/conf.d/90-nwp-moodle.ini` (same for `-cli`), then `sudo systemctl reload php8.2-fpm`. **Remove the floor entirely** (returns both SAPIs to the PHP default 1000 and re-creates the 2026-07-26 outage condition — do this only to prove causation): `sudo rm -f /etc/php/8.2/{fpm,cli}/conf.d/90-nwp-moodle.ini && sudo systemctl reload php8.2-fpm`, then confirm with `pl server-state php-check live` (must go RED). |
| CP-20260802-metnightlypl | 2026-08-02 | **ops#156 / ruling D15 — met's nightly becomes a `pl` verb.** `demo_box_endpoint` + `demo_box_ssh_args` (one source of truth for the restricted-key transport), `pl demo nightly <site> --tier=live --via-key [--host] [--dry-run] [--print-transport]`, `pl demo schedule --via-key` now writes the pl-mediated cron line (`--raw-ssh` writes the previous bare-ssh line), `_token_present` in `lib/gitlab-issues.sh`, host-override on `harvest-pull`, 13 new bats tests + 3 updated, guide §13. **Repo-only: no live site, server, DB, credential or `private/` state touched, THE BOX WRAPPERS WERE NOT EDITED OR RE-INSTALLED, AND NO CRONTAB WAS WRITTEN ON ANY HOST** — met's crontab is byte-unchanged and still runs the bare-ssh line (backup at `private/demo/crontab.met.pre-ops156-20260802.bak`). Host contact was read-only plus two `dry-run` action words against the box (the wrapper's own no-op path; box log records `dry-run-ok`, nothing changed). Net effect while met's crontab is untouched: **none** — tonight's 01:00–03:30 Melbourne nightly runs exactly the code and command it ran last night. | met crontab verbatim copy at `private/demo/crontab.met.pre-ops156-20260802.bak` (untracked, on the dev workstation) | branch `feat/met-nightly-via-pl` off `origin/main` `4dfbda7` | `git revert -m 1 <merge>` in nwp/nwp — removes a verb and a flag; restores no state, because none was changed. If the crontab HAS since been cut over on met and the new form misbehaves: `~/nwp/pl demo schedule <site> --tier=live --via-key --raw-ssh --host gitlab@<box>` reinstalls the exact previous line, or `crontab private/demo/crontab.met.pre-ops156-20260802.bak` restores met's whole crontab verbatim. |
| CP-20260802-mailid-ss | 2026-08-02 | **live `ss` mail identity set from a declared fact (D7)** — `$CFG->supportname` `Admin User` → `Saint School Support` and `$CFG->noreplyaddress` `noreply@<ESTATE>` → `ss@<ESTATE>`; `supportemail` was already `support@<ESTATE>` and was left unchanged. **Three `mdl_config` rows and nothing else**: no course, user, plugin, file or credential was touched, and `noemailever`/`divertallemailsto` are declared out of scope by the verb's impact manifest. Written through `pl moodle mail ss --tier=live --apply` (ADR-0028 deploy gate + typed impact confirm) and verified by re-read of all three fields. The declaration lives in the gitignored `sites/ss/.nwp.yml` `mail:` block. | pre-change values are recorded verbatim in this row and in the `sites/ss/.nwp.yml` comment block; a byte copy of the pre-change site config is at `/tmp/claude-1000/-home-rob-nwp/f33b1a66-1ac4-4e5d-914a-e8826febc93d/scratchpad/ss.nwp.yml.bak`. No DB snapshot was taken — the change is three `mdl_config` scalars whose prior values are written down here, which is a cheaper and more exact recovery path than a 20 MB dump. | branch `feat/site-mail-identity-declared` off `origin/main` `4dfbda7` (MR !311) | Edit `sites/ss/.nwp.yml` `mail:` back to `support_name: Admin User` / `noreply_address: noreply@<ESTATE>`, then `pl moodle mail ss --tier=live --apply`. Or set the two values directly: `pl moodle cli ss --tier=live --execute -- admin/cli/cfg.php --name=noreplyaddress --set=noreply@<ESTATE>` (same for `supportname`). Reverting MR !311 removes the verb but does **not** revert live — the values are site config, not code. |
| CP-20260802-mailid-ssd | 2026-08-02 | **live `ssd` mail identity + GOLDEN re-assertion (D7)** — `supportemail` `support@<ESTATE>` → `demo-support@<ESTATE>`, `supportname` `Admin User` → `Saint School Demo`, `noreplyaddress` `noreply@<ESTATE>` → `ssd@<ESTATE>`, via `pl moodle mail ssd --tier=live --apply --sync-golden`. Because the box's nightly forced command re-imports `mdl_config` wholesale from the golden, the same three facts were also appended to `sites/ssd/demo-golden-live/golden.db.sql.gz` as an idempotent `INSERT … ON DUPLICATE KEY UPDATE` block between `-- >>> nwp:mail-identity` markers, and the image was re-staged to the box. **The golden was APPENDED TO, never recaptured and never rewritten in place**: the decompressed diff against the prior image is exactly six added lines, so the 55-course catalogue captured at 17:06Z is byte-identical. `noemailever=1`, `divertallemailsto` and `nwp_demo_mode=1` were not touched. PROVEN by a real `pl demo reset ssd --tier=live` (51 s, the same path the 01:15 cron runs): all three values survived the restore. | prior golden (db sha256 `e59789a35b652a36…`, files sha256 `9774fc0d4dc33a36…`, captured `2026-08-01T17:06:57Z`) kept at `/tmp/claude-1000/-home-rob-nwp/f33b1a66-1ac4-4e5d-914a-e8826febc93d/scratchpad/golden-backup/` — all five files incl. manifest and sidecars. New db sha256 `4e9e3784eb52dddecc7a6bab2f280c028f3ec57968a92d0dc71243619c5754e4`; files tarball unchanged. Pre-change site config at `/tmp/claude-1000/-home-rob-nwp/f33b1a66-1ac4-4e5d-914a-e8826febc93d/scratchpad/ssd.nwp.yml.bak`. | branch `feat/site-mail-identity-declared` off `origin/main` `4dfbda7` (MR !311) | Live half: as CP-20260802-mailid-ss, with `demo-support@`/`ssd@` reverted to `support@`/`noreply@`. Golden half, in order of preference: (1) revert the `mail:` block and re-run `pl moodle mail ssd --tier=live --apply --sync-golden`, which rewrites the managed block idempotently; (2) delete the lines between the `-- >>> nwp:mail-identity` / `-- <<< nwp:mail-identity` markers by hand and re-run `sha256sum golden.db.sql.gz > golden.db.sql.gz.sha256` plus a manifest `db_sha256` update; (3) restore `/tmp/claude-1000/-home-rob-nwp/f33b1a66-1ac4-4e5d-914a-e8826febc93d/scratchpad/golden-backup/` over `sites/ssd/demo-golden-live/`. In every case finish with `bash servers/live/demo/install-box.sh ssd --stage-golden`, or the box keeps serving the old image. |
| CP-20260802-ops226-notify | 2026-08-02 | **ops#226 — every Sojourner level grant threw, so nobody was congratulated.** `nwp/nwc` only: new `nwc_guild.skill_naming` service (`SkillNamingService`), `SojournerProgressionService::SKILL_KEY`, `hook_nwc_guild_level_granted` docblock widened to `TermInterface\|string`, `nwc_notification`'s implementation given a native union type hint and made to resolve through the new service, plus `LevelGrantedNotificationTest` (4 cases). **No live site, server, DB, credential, golden or `private/` state was touched.** Verification ran on the LOCAL `nwd-dev` DDEV copy and its test data was deleted afterwards (8 completion rows, 2 notification entities, 2 user.data levels); the checkout was `git reset --hard` back to `584dc12` and caches rebuilt. **Live nwd was only READ** (`pl drush nwd --tier=live --execute -- watchdog:show`), which confirmed the bug firing twice today. Net effect while unmerged AND undeployed: none — live nwd still swallows the error. | — (no host state changed) | `nwp/nwc` branch `ops-226-sojourner-level-notification` off `origin/main` `5def9fd` | `git revert -m 1 <merge>` in nwp/nwc, then redeploy the profile. Reverting restores the pre-fix behaviour (levels still recorded, notifications still silently dropped) and loses no state — the fix writes nothing that a revert would strand. To keep the fix but stop the new service being used, delete the `nwc_guild.skill_naming` block from `nwc_guild.services.yml`: the hook's `\Drupal::hasService` guard then takes its explicit fallback branch and still emits the notification, just with a generic `Level N` rung name. |
| CP-20260802-ops226-goldenupdates | 2026-08-02 | **ops#226 follow-on — a golden may no longer be captured while a database update is pending.** `nwp/nwp` only: `demo_pending_updates_verdict` / `_check_local` / `_check_live` in `scripts/commands/demo.sh` wired at step 0 of BOTH capture paths (`cmd_golden`, `cmd_golden_live`, and therefore `cmd_golden_paired`), a new `pending_db_updates` attestation in `demo_manifest_write` (`lib/demo.sh`), and post-restore detection `[G10]` in `servers/live/demo/nwd-demo-reset-restricted`; 9 + 3 new bats cases. **Repo-only: NO golden was captured or recaptured, no site, server, DB, crontab or `private/` state was touched, and the box wrapper was NOT re-installed** — the edited wrapper is the versioned copy; `/usr/local/bin/nwd-demo-reset-restricted` is unchanged until someone runs `install-box.sh`. Net effect while unmerged: none. Once merged: `pl demo golden` REFUSES (no override flag) on a Drupal site with pending `hook_update_N`, or on one whose update state cannot be read. Once merged AND deployed: a nightly that restores a pending-update golden logs `reset-pending-updates` and exits 1 instead of `reset-ok`. Moodle is explicitly OUT of scope and says so at runtime. | — (no host state changed; the staged `nwd`/`ssd` goldens were neither read nor written) | branch `ops-226` off `origin/main` `fe2d7d2` | `git revert -m 1 <merge>` in nwp/nwp — removes two refusals and one manifest field; restores no state. To keep the capture gate but stop the reset degrading a run, re-install the previous wrapper: `git show <merge-base>:servers/live/demo/nwd-demo-reset-restricted` then `bash servers/live/demo/install-box.sh nwd`. There is deliberately **no** `--allow-pending-updates`: a config gap can be a considered trade-off, a pending update inside a golden cannot, because the nightly undoes the remedy. A manifest written before this change reads `pending_db_updates: unknown`, which is NOT a clean bill and is what the reset-side check exists to cover. |
| CP-20260802-completionbridge-code | 2026-08-02 | **ssd→nwd completion bridge — REPO HALF (two repos, both UNMERGED).** nwp/nwp: two new demo-tier provisioners (`scripts/demo/ssd-completion-ws-provision.{sh,php}`, `scripts/demo/ssd-completion-criteria.{sh,php}`), `tests/unit/test-ssd-completion-bridge.bats` (17 cases, read-only-surface guard mutation-proven), `docs/guides/ssd-nwd-completion.md`. nwp/nwc: `SojournerCompletionPull` + two read methods on `MoodleDataService` + 3 config keys (shipped OFF) + `nwc_moodle_data_update_10001` + `hook_cron` + 2 drush commands + 6-case unit test. **`lint:yq-first` exits 0 with NO baseline row added.** Repo-only: this row covers the CODE, not the live writes (next three rows). | — (no host state changed by the code itself) | nwp/nwp branch `feat/ssd-nwd-completion-bridge` off `origin/main` `cad29cf`; nwp/nwc branch `feat/ssd-nwd-completion-pull` off `origin/main` `5def9fd`. NEITHER MERGED. | `git revert -m 1 <merge>` in each repo. Both are additive: the nwc module code is inert until BOTH `enable_completion_pull` AND `nwc_demo_access:demo_mode` are true, and the nwp scripts are dry-run by default and refuse any site whose pair contract lacks `demo.enabled: true`. Reverting nwc removes the puller class and the cron hook; the evidence rows already written stay (they are ordinary `nwc_sojourner_completion` rows and `pl drush nwd --tier=live --execute -- sqlq "DELETE FROM nwc_sojourner_completion WHERE source='moodle_pull'"` removes them). To disable WITHOUT reverting: `pl secrets inject` a `settings.local.overrides.php` without the `enable_completion_pull` line, or set it to `0`. |
| CP-20260802-completionbridge-ssdws | 2026-08-02 | **ssd LIVE: read-only completion web service provisioned.** Created role `nwdcompletionreader` (id 9, CONTEXT_SYSTEM, no archetype), service account `nwd_completion_reader` (id 7, `auth=webservice`, cannot log in interactively), external service `nwd_completion_pull` (id 2, `enabled=1`, `restrictedusers=1`) with exactly three READ functions, one authorisation row, one permanent token (id 1). Capabilities granted: `webservice/rest:use`, `moodle/user:viewdetails`, `moodle/site:viewuseridentity`, `moodle/course:viewparticipants`, `moodle/course:view`, `report/completion:view`; PREVENTed: `moodle/course:viewhiddencourses`, `moodle/course:viewhiddenuserfields`. **One site setting changed: `showuseridentity` `'email'` → `'email,idnumber'`** (Moodle only searches the fields listed there, so the idnumber lookup silently returned `[]` without it). Credential registered as `private/secrets-registry.yml` id `ssd_nwd_completion_ws` (#35) — value never printed, never in git. NO write function is exposed; a negative probe asserts `core_user_update_users` → `accessexception`. | pre-change ssd LIVE DB snapshot `box:/home/gitlab/nwp-snapshot-ssd-moodledb-20260802-150138.sql.gz` (taken by `pl moodle backup ssd --tier=live --db-only --apply`, rollback point `ssd@live moodle-remote 20260802-150138`) | — | `pl moodle rollback ssd --tier=live execute` restores the whole DB. Item-scoped undo instead: delete the `{external_tokens}` row for service `nwd_completion_pull`, then the `{external_services_users}` + `{external_services_functions}` + `{external_services}` rows, then `delete_role(9)` and delete user `nwd_completion_reader`, and set `showuseridentity` back to `'email'` (`admin/cli/cfg.php --name=showuseridentity --set=email`). Re-provision at any time with `scripts/demo/ssd-completion-ws-provision.sh --tier=live --apply`; `--check` reports drift including any UNEXPECTED capability or function. |
| CP-20260802-completionbridge-ssdcriteria | 2026-08-02 | **ssd LIVE: 40 course-completion criteria added to 9 courses** — first batch 23 (B1×4, B2×5, B3×6, B4×4, D1×4), second batch 17 (A1×4, A4×4, A5×4, A6×5) added during the L1→L2 re-proof, type `COMPLETION_CRITERIA_TYPE_ACTIVITY`, aggregation `ALL`, plus one `{course_completion_aggr_methd}` row per course. This is the upstream defect, not a preference: `{course_completion_criteria}` had **0 rows site-wide** across all 59 courses, so `completed` was permanently false even at `progress=100` and no completion could ever flow. **Scope was deliberately limited to 5 named courses** — `--courses` is required and there is no `all` shortcut, because ssd course content is owned elsewhere. Also written as a consequence: `{course_completions}` rows for uid 6 (B1–B4 complete) and self-enrolments for uid 6 in B1–B4, both produced by walking the tester journey, both throwaway demo state. | same snapshot as the row above: `box:/home/gitlab/nwp-snapshot-ssd-moodledb-20260802-150138.sql.gz` | — | `pl moodle rollback ssd --tier=live execute`. Item-scoped: `DELETE FROM {course_completion_criteria} WHERE course IN (60,61,62,63,71,80,110,111,112)` and the matching `{course_completion_aggr_methd}` rows; that alone re-breaks completion (returns the site to its pre-change behaviour). The `{course_completions}` rows for uid 6 are demo state and are wiped by the next `pl demo reset` regardless. Re-apply with `scripts/demo/ssd-completion-criteria.sh --tier=live --courses=B1,B2,B3,B4,D1,A1,A4,A5,A6 --apply`. **NOTE for whoever restores more ssd content:** existing completions are safe — ALL-aggregation is over the DECLARED criteria rows, not over every activity, so adding activities to a course does not re-open it. But re-running this script with `--apply` after such a restore WILL declare criteria for the new activities and CAN re-open a previously complete course. Run `--check` first. |
| CP-20260802-completionbridge-nwdinject | 2026-08-02 | **nwd LIVE: `settings.local.overrides.php` CREATED** by `pl secrets inject nwd --tier=live --apply` with three `$config['nwc_moodle_data.settings']` overrides — `webservice_token` (secret, never printed), `moodle_url` = `https://ssd.nwpcode.org`, `enable_completion_pull` = `"1"`. Mode 440 www-data, rsync-excluded, `include`d by the existing `settings.local.php` tail. **The file did not exist before, so nothing was overwritten.** Chosen over Drupal DB config deliberately: the nwd demo golden captures only `sites/default/files`, so DB config is wiped nightly while this file is not — the bridge therefore needs NOTHING in the golden image. `drush cr` ran (the inject verb's own attempt failed on the drush-path issue and was re-run as `pl drush nwd --tier=live --execute -- cr`). Also written by the proof run: 4 `nwc_sojourner_completion` rows for uid 21 (`source=moodle_pull`) and `users_data nwc_guild:sojourner_level` = 1 for uid 21. | the file is NEW — the rollback is deletion, not restoration. Pre-change nwd DB baseline for the proof rows: the overnight-B snapshot in `CP-20260802-onb1` (`box:/home/gitlab/nwp-snapshot-nwd-dbs-20260802-015007.sql.gz`) | — | Remove the overrides: `ssh` is not the verb — re-run `pl secrets inject nwd --tier=live --apply` after deleting the three `config:` items from the `inject:` block in `private/secrets-registry.yml` (it rewrites the file in full), or set `enable_completion_pull` to `"0"` there and re-inject, which disarms the pull and leaves the token in place. To undo the proof rows: `pl drush nwd --tier=live --execute -- sqlq "DELETE FROM nwc_sojourner_completion WHERE user_id=21 AND source='moodle_pull'; DELETE FROM users_data WHERE module='nwc_guild' AND name='sojourner_level' AND uid=21"`. Both are throwaway demo state and are wiped by the next `pl demo reset nwd` regardless. To revoke the credential entirely see `CP-20260802-completionbridge-ssdws`. |
| CP-ops230-oversight | 2026-08-02 | **ops#230 oversight un-blinding** — (a) REPO: capability-keyed gate split in `lib/loop-parts.sh` (WRITE kill `.loop-paused`/`all=disabled` no longer stops `observe` parts; new OVERSIGHT kill `.oversight-paused`/`oversight=disabled`), new `lib/oversight-freshness.sh` probe, new `lib/rag-sync-plan.py` (planner extracted from a heredoc in `rag.sh`, plus the `absent` fix), ops#204 duplicate `check_rag_sync_freshness` resolved, `pl loop schedule` verb, RSY→RED in `lib/rag-render.py`, `get_todo_setting` no longer swallows `false`. **No live site, server, DB or credential was touched** — every probe is a local file read. (b) HOST STATE, dev workstation `carlo`: the user crontab was EMPTY and gained ONE managed block installing the daily `rag-sync` cron (`30 4 * * *`). Nothing else on any host was modified; `.loop-paused` was deliberately LEFT IN PLACE (it is the write kill and belongs to the operator). | pre-change crontab byte copy written by the verb itself before it wrote: `private/rollback/ops230/crontab.20260802T083113Z.bak` (empty file — the crontab genuinely had no entries) | branch `ops-230-oversight-freshness` off `origin/main` `fe2d7d2`, MR to `nwp/nwp` | REPO: `git revert -m 1 <merge>`. To keep the code but restore the pre-ops#230 single-kill semantics without a revert: `NWP_LOOP_UNIFIED_GATES=1` (honoured everywhere, logged loudly by the wrapper and by `pl loop`). To silence the new freshness finding without reverting: `settings.todo.categories.rag_sync: false` in `nwp.yml` — note this now genuinely works, where before the fix a `false` here was swallowed and every category was permanently on. HOST: `pl loop schedule remove --execute` (removes exactly the managed block, by marker), or `crontab private/rollback/ops230/crontab.20260802T083113Z.bak` to restore the empty crontab verbatim. Reverting the repo half does NOT remove the cron entry, and removing the cron entry does not revert the repo half — they are independent on purpose. |
| mini-agent-host | 2026-08-02 | **mini provisioned as the durable agent host.** (a) REPO: `pl secrets provision` (new verb), `pl secrets adopt <path>:<ref>` (local-file locations), ssh probes in `_probe_scopes`, `tests/unit/test-secrets-provision.bats` (29 tests), `docs/guides/durable-agent-host.md`. **No live site, server or database was written by the repo half** — the only host contact from the code is read-only (`ssh … true` probes, `sha256sum` read-backs). (b) REGISTRY (`private/secrets-registry.yml`, gitignored): 1 new entry `ssh_live_box_gitlab`; 1 new `stored_in` row on `gitlab_bot_ops_note` (`host=mini:…`); notes updated on both. (c) MINI host state: yq v4.50.1 installed to `~/.local/bin` (sha256 `c7a1278e6bbc4924…`, byte-identical to the laptop's); a PATH block prepended to `~/.bashrc`; 3 host keys for the live box appended to `~/.ssh/known_hosts` (fingerprints cross-checked against the laptop's trusted record BEFORE install, not blind-accepted); `~/nwp` fast-forwarded `27f2830`→`fe2d7d2`; `~/nwp/.secrets.yml` CREATED (0600, one credential + one structural key); `nwp.yml`, `servers/{live,nwpcode}/.nwp-server.yml`, `sites/nwd/.nwp.yml` copied (topology, no credentials); tmux session `nwp` + `~/.config/systemd/user/nwp-tmux.service` + linger. **Nothing prod-reaching was provisioned, by design and by code** — `provision` refuses `ver`/`mons` with no override. | registry pre-change copy `private/rollback/mini-agent-host/secrets-registry.pre-mini-agent-host.yml` sha256 `bc1d40e1fbd8f9ced9796eefdf087d7079bd72d14c30de870f0c62469cd13624` (off-host; `private/` is gitignored). On mini: `~/.bashrc.bak-mini-host-20260802T080229Z`, `~/.ssh/known_hosts.bak-mini-host-20260802T074409Z`, `~/nwp/nwp.yml.bak-mini-host-20260802T081307Z` | branch `feat/mini-agent-host` off `origin/main` `fe2d7d2` | REPO: `git revert -m 1 <merge>` — every change is a NEW verb, a STRICTER check or a doc, so reverting removes capability and restores no state. REGISTRY: `cp private/rollback/mini-agent-host/secrets-registry.pre-mini-agent-host.yml private/secrets-registry.yml`. MINI: `cp ~/.bashrc.bak-mini-host-* ~/.bashrc` · `cp ~/.ssh/known_hosts.bak-mini-host-* ~/.ssh/known_hosts` · `cp ~/nwp/nwp.yml.bak-mini-host-* ~/nwp/nwp.yml` · `git -C ~/nwp reset --hard 27f2830` · `rm ~/.local/bin/yq` · `rm -f ~/nwp/.secrets.yml` (revokes mini's board access; the token itself stays valid elsewhere — to revoke the CREDENTIAL rotate `gitlab_bot_ops_note`) · `systemctl --user disable --now nwp-tmux.service && tmux kill-session -t nwp && loginctl disable-linger rob`. Reversing the registry rows without reversing the host leaves an UNDECLARED copy on mini — the met defect. Do the registry restore and the `rm ~/nwp/.secrets.yml` together, or neither. |
| CP-ops231-nw1 | 2026-08-02 | ops#231 nw1 — drupal/core 10.6.12→10.6.14, guzzlehttp/guzzle 7.12.3→7.15.2 (dev tree). Closes 6 advisories (SA-CORE-2026-012 CVE-2026-55805, SA-CORE-2026-010 CVE-2026-15916, + the four guzzle advisories of 2026-07-20). COMMITTED in sites/nw1/dev @ 418b7ac (composer.lock only; that repo has no git remote, so the commit is local-only). NO live site, server, DB or credential was touched — dev DDEV tree only. Verified installed (not merely locked): vendor guzzle 7.15.2, drush reports core 10.6.14, HTTP 200. | `docs/reports/consolidation-arc-2026-07/code-snapshots/ops231-nw1-composer-pre-20260802.tar.gz` (sha `99a80e06…`, sidecar alongside) | — | git -C sites/nw1/dev revert --no-edit 418b7ac && (cd sites/nw1/dev && ddev composer install). Artifact route if the commit is gone: tar -xzf docs/reports/consolidation-arc-2026-07/code-snapshots/ops231-nw1-composer-pre-20260802.tar.gz -C sites/nw1/dev && (cd sites/nw1/dev && ddev composer install) |
| CP-ops231-nwd | 2026-08-02 | ops#231 nwd — drupal/core 10.6.13→10.6.14, guzzlehttp/guzzle 7.12.3→7.15.2 (dev tree). Closes the four guzzle advisories of 2026-07-20; 4 active → 0. NOT COMMITTED: sites/nwd/dev already carried an unrelated, 12-day-old uncommitted rewrite of composer.json from another session, and composer.lock's content-hash is bound to that modified composer.json, so committing the lock alone would produce a broken pair. The change therefore lives in the working tree + vendor/ only. NO live site, server, DB or credential touched — dev DDEV tree only. Verified installed (not merely locked): vendor guzzle 7.15.2, core 10.6.14, HTTP 200. | `docs/reports/consolidation-arc-2026-07/code-snapshots/ops231-nwd-composer-pre-20260802.tar.gz` (sha `b1008c19…`, sidecar alongside) | — | tar -xzf docs/reports/consolidation-arc-2026-07/code-snapshots/ops231-nwd-composer-pre-20260802.tar.gz -C sites/nwd/dev && (cd sites/nwd/dev && ddev composer install). This restores composer.json AND composer.lock to the exact bytes found at 2026-08-02T17:55Z, including the other session's uncommitted composer.json work. |
| CP-ops231-nwc | 2026-08-02 | ops#231 nwc — drupal/core 10.6.13→10.6.14, guzzlehttp/guzzle 7.12.3→7.15.2 (dev tree). Closes the four guzzle advisories of 2026-07-20; 4 active → 0. html/core had been installed FROM SOURCE and was dirty with 35 patch-applied files, so composer refused to replace it and left lock ahead of vendor (a false-green risk); html/core was deleted and reinstalled from dist, patches re-applied clean. html/core is gitignored by the site repo, so no tracked work was at risk. NOT COMMITTED: composer.json and composer.lock both carried another session's uncommitted work from 2026-07-21 (captcha/honeypot/riddler/simple_sitemap). NO live site, server, DB or credential touched — dev DDEV tree only. Verified installed: vendor guzzle 7.15.2, drush reports core 10.6.14, HTTP 200. | `docs/reports/consolidation-arc-2026-07/code-snapshots/ops231-nwc-composer-pre-20260802.tar.gz` (sha `37e253dc…`, sidecar alongside) | — | tar -xzf docs/reports/consolidation-arc-2026-07/code-snapshots/ops231-nwc-composer-pre-20260802.tar.gz -C sites/nwc/dev && (cd sites/nwc/dev && rm -rf html/core && ddev composer install). Full pre-change html/core (428M, incl. its source .git) and vendor/ are in the session scratchpad as nwc-html-core-pre-ops231.tar.gz and nwc-vendor-pre-ops231.tar.gz — NOT durable, they die with the scratchpad; the composer route above is the durable one and reproduces html/core exactly from the lock. |
