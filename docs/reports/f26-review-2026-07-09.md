# F26 OIDC (nwc ↔ ss) — operator review brief

**Date:** 2026-07-09
**Branch:** `feat/f26-oidc-nwc-ss` (MR left **OPEN**, do **not** auto-merge)
**Base:** `main` @ `89f53c4` · **Rollback anchor tag:** `pre-f26-2026-07-09` (= `89f53c4`)
**Scope owned by this build:** `nwc-dev` only. No prod, no mons, no shared Moodle site reconfigured.

> ## ⚠️ AUTH — REQUIRES HUMAN REVIEW BEFORE MERGE / ENABLE
> This is an **authentication surface**. Per CLAUDE.md (Auth changes = High-Risk /
> Block-and-Escalate), F26 §4.4 (1 approver during build-out, 2 once live), and
> P72 §5 (the nwc↔ss edge is a *new auth-graph edge F26 explicitly deferred* and
> needs the same sign-off), **an agent may draft/build on a branch but may not
> merge or enable this.** Nothing here has been merged. The Drupal issuer changes
> were applied only to the local `nwc-dev` DDEV site for verification and are
> fully reversible (§ Rollback).

---

## 0. UPDATE 2026-07-09 — the two review recommendations are now APPLIED

Operator approved "F26 = all recs". Both §5 recommendations (a) and (b) have been
implemented on the F26 branch + the nwc profile repo and verified on `nwc-dev`.
Still **OFF / not enabled on any site**; MR **!49 stays OPEN** for human review.

**Rollback anchor for the recs:** tag `pre-f26-recs-2026-07-09` = F26 tip
`6010aab` (rollback SHA `6010aab23341e46937a3dadff5162e3371581289`).

### Rec (a) — drop the custom `/oauth/userinfo` route (DONE)
Changes in the **nwc profile repo** (`nwp/nwc`, branch
`feat/f26-drop-custom-userinfo`):
- Removed the custom `nwc_moodle_oauth.userinfo` route + `UserInfoController.php`
  (the weak `loadByProperties(['value' => $token])` manual-Bearer lookup). The
  **native** `simple_oauth.userinfo` now solely binds `/oauth/userinfo`.
- Removed the redundant `hook_simple_oauth_oidc_claims_alter()` in
  `nwc_moodle_oauth.module` (it wrote a flat `guilds` map that collided, on the
  same claim key, with `nwc_oidc_claims`' structured shape — winner depended on
  module-name order).
- **Fixed a latent bug this exposed:** `nwc_oidc_claims`' hook was type-hinted
  `(array &$claims, UserInterface $account)` — the wrong signature for
  simple_oauth 6.x, which passes `(array &$claim_values, array &$context)`. It
  had never actually run because the custom controller shadowed the route.
  Corrected to read `$context['account']`.
- **Registered the `guilds` claim** via a new `NwcOidcClaimsServiceProvider`
  (simple_oauth filters userinfo to the `simple_oauth.openid.claims` allow-list;
  custom claims are dropped unless registered — per `simple_oauth.api.php`). The
  provider also re-asserts the standard profile claims (see rec (b) note).

Verified on `nwc-dev`: native `/oauth/userinfo` returns **HTTP 200** with
`sub`, `email`, `name`, `preferred_username`, … **and** the structured `guilds`
claim (8 guilds w/ roles) for a valid token; **401** for an invalid bearer;
no-token requests are bounced to login (307, no claim leak).

### Rec (b) — add OIDC discovery (DONE)
- `composer require e0ipso/simple_oauth_21:^1.13` (installed **v1.13.0**) and
  enabled `simple_oauth_21` + `simple_oauth_server_metadata` on `nwc-dev`.
- `/.well-known/openid-configuration` now serves valid metadata: `issuer`,
  `jwks_uri`, `authorization_endpoint`, `token_endpoint`, `userinfo_endpoint`,
  `RS256`, etc.
- INSTALL.md updated to **prefer Moodle auto-discovery** (set Service base URL →
  Moodle discovers endpoints), with manual entry kept as fallback.

**Discovery — supply-chain note (operator decision before enable):**
`simple_oauth_server_metadata` is a **submodule of `e0ipso/simple_oauth_21`**,
**not** the standalone `drupal/simple_oauth_server_metadata` the review guessed
(that package does not exist on packages.drupal.org). `simple_oauth_21` is **not
published on packages.drupal.org** either, so it was installed via a **git VCS
composer repo pinned to tag `v1.13.0`** (`no-api` clone, no GitHub token). It is
by **e0ipso (Mateu Aguiló Bosch), the maintainer of `simple_oauth` itself** — so
same provenance as an already-trusted, already-installed dependency. Per the NWP
threat model, for any non-dev environment **mirror it into `git.<gitlab-host>`**
rather than pulling from GitHub at deploy time. Because `sites/nwc/dev/` is
gitignored in `nwp/nwp`, this composer change lives only on the local dev site,
not in MR !49.

**Side effect handled:** enabling `simple_oauth_server_metadata` **redefines** the
`simple_oauth.openid.claims` container parameter to only the JWT registered
claims, which would strip `name`/`email`/`preferred_username` from userinfo (and
break Moodle field mapping). `NwcOidcClaimsServiceProvider` re-asserts the
standard profile claims (union) so userinfo returns the full expected set.

Verification scripts (both green on `nwc-dev`):
`scripts/f26/verify-nwc-issuer.sh` → **6/6** (adds discovery + anon-gate),
`scripts/f26/verify-native-userinfo.sh` → **3/3** (mints a dev token, asserts
native userinfo claims + guilds, invalid-token 401).

---

## 1. What F26 is, and the nwc↔ss decision

F26 as written makes **AVC** the OIDC issuer and **SS** (Moodle) the client, to
give AVC↔SS single sign-on. P72 needs the identity to resolve into **nwc**
(the canonical 2.0 site that owns the editorial pipeline and guild membership),
which F26 lists as an explicit **non-goal** ("adding another site … would require
a separate proposal").

**Decision taken (logged §6): P72 §3.2 _shape 1_ — nwc becomes the issuer for ss.**
Rationale: the identity that must land on the nwc editorial "propose" form is an
**nwc member**, so making nwc the issuer means the `sub` in the ID token *is* the
nwc uid with no second reconciliation hop. This also matches the substantial
**pre-existing** nwc-side code (already nwc-branded, see §2). Shape 2 (nwc as a
second relying party behind an AVC issuer) was rejected as more moving parts for
no benefit to P72. **This choice is exactly the auth-graph edge that needs your
sign-off.**

## 2. Prior work found (not built by this session)

A large amount of F26 was already present and is *reused*, not rebuilt:

| Piece | Location | State |
|---|---|---|
| `simple_oauth` 6.1.1 | `nwc/dev html/modules/contrib` | enabled, OIDC on |
| `nwc_moodle_oauth` (userinfo + guild claims) | nwc profile repo | enabled |
| `nwc_oidc_claims` (guild claims via hook) | nwc profile repo | enabled |
| `nwc_moodle_sync`, `nwc_moodle_data` | nwc profile repo | enabled |
| RS256 signing keys | `nwc/dev/oauth-keys/` | present (local, **not** from prod) |
| `lib/sanitizers/oidc-email.sh` (F26 Phase 3) | main repo | **already committed** (77b16c1), 15 bats tests pass |
| Moodle `auth/avc_oauth2` plugin | `sites/ss/dev`, `sites/ss2/dev` | AVC-branded, **no UID-lock** |

Two problems in the prior nwc code (flagged, not changed — auth-gated):
- **Route collision:** both `simple_oauth.userinfo` and `nwc_moodle_oauth.userinfo`
  bind `/oauth/userinfo`. simple_oauth 6.x ships a native userinfo; the custom
  one is redundant and does its own manual Bearer parse. Recommend deleting the
  custom route/controller and letting `nwc_oidc_claims` (correct hook) add guild
  claims to the native endpoint.
- The custom `UserInfoController` looks tokens up by raw `value`; native
  simple_oauth handling is preferable. Left for your review.

## 3. What this session built (on the branch)

| Artifact | Runnable? | Notes |
|---|---|---|
| `scripts/f26/provision-nwc-issuer.sh` | **Yes — ran on nwc-dev** | idempotent: registers keys, ensures OIDC + `openid/profile/email` scopes, creates a **confidential, PKCE-required** `ss_moodle` client with redirect URIs to `ss-dev`/`ss2-dev` `/admin/oauth2callback.php`, generates a per-client secret (gitignored). |
| `scripts/f26/verify-nwc-issuer.sh` | **Yes — 4/4 pass** | JWKS serves a key; `/oauth/userinfo` → 401 anon; `/oauth/authorize` routes (400 not 404); client is confidential+PKCE with 3 scopes. |
| `scripts/f26/moodle/auth_nwc/classes/uid_lock.php` | **Yes — pure logic, 10/10 unit tests** | the F26 §3.2 UID-lock decision, Moodle-free so it is unit-testable. |
| `scripts/f26/moodle/auth_nwc/tests/uid_lock_logic_test.php` | **Yes — `php` runs it, 10/10** | covers create / reuse-locked / idnumber-beats-email / migration-link / migration-off / suspend / deny-empty-sub branches. |
| `scripts/f26/moodle/auth_nwc/{auth,settings,version}.php`, `lang/…` | **No — needs a Moodle env** | thin plugin riding Moodle core OAuth2, applies the lock via `resolve_and_lock()`. |
| `scripts/f26/moodle/INSTALL.md` | doc | exact install/enable/config + smoke-test + rollback. |

### Runnable-vs-spec summary

- **Runnable & verified on nwc-dev:** issuer key registration, OIDC scopes, the
  `ss_moodle` confidential+PKCE client, all four provider endpoints, the anon-gate
  on userinfo, and the entire UID-lock *decision* logic (unit-tested).
- **Config-only / needs a Moodle env to verify:** the `auth_nwc` client plugin
  execution and the end-to-end browser hand-off. A Moodle DDEV site **does**
  exist (`ss-dev`, `ss2-dev`), but this build **exclusively owns `nwc-dev`** and
  deliberately did **not** reconfigure a shared Moodle site's auth (that would be
  an auth-surface change on a site it does not own and could disrupt other
  sessions). Apply `INSTALL.md` against a Moodle you own to finish verification.

## 4. Security model (no shortcuts)

- **Flow:** standard OIDC **authorization-code + PKCE (S256)**. The `ss_moodle`
  client is **confidential** with a **per-client secret generated at provision
  time** (not a shared static token), stored hashed by simple_oauth and written
  locally to a **gitignored** file — never committed. There is **no shared-secret
  hand-off, no bearer token in a deep-link, and no anonymous auto-create** (the
  exact shortcuts P72 §3.3 / §5 forbid).
- **UID-lock:** `mdl_user.idnumber` == nwc uid (`sub`). After first lock, the user
  is resolved by `idnumber`, **never by email**; email/name are refreshed but the
  lock never changes (F26 §3.2). Verified in unit tests ("idnumber beats email").
- **`sub` is trusted, not user input:** it is the subject of an ID token signed by
  the nwc issuer (RS256) and verified by Moodle core OAuth2 against nwc's JWKS.
  An empty/unsigned `sub` → **deny** (never create/guess).
- **Legacy email link is one-time and gated:** binding a pre-OIDC Moodle account
  by email happens only when `link_legacy_by_email` is ON *and* the ID token says
  `email_verified === true`. Because the email is asserted by the trusted issuer
  (not typed by the client), this is not an account-takeover vector. Turn the flag
  OFF outside a migration window.
- **Signing keys are per-site, never from prod:** the `oauth-keys/` on nwc-dev
  were generated locally. F26 §4.1 makes "never copy the prod signing key into a
  preview/dev" load-bearing; this build honours it and the provision script never
  copies keys from anywhere.
- **Deterministic sanitizer email-hash** (F26 §3.3) is already in tree
  (`lib/sanitizers/oidc-email.sh`): `sha256(email‖salt)[:16]@sanitized.test`, salt
  in `.secrets.data.yml` (AI cannot read it), never rotated (F26 §4.2). Salt never
  logged; empty input → empty output. 15 bats tests pass.

## 5. What you must check / tweak before enabling

1. **Approve the nwc↔ss auth edge itself** (the §1 decision) — this is the review
   F26 §4.4 / P72 §5 require. Everything else is downstream of that yes/no.
2. ~~**Resolve the `/oauth/userinfo` route collision**~~ — **DONE (rec a, §0).**
   Native simple_oauth userinfo now serves the path; `nwc_oidc_claims` (fixed)
   injects guilds. Review the nwc-profile branch `feat/f26-drop-custom-userinfo`.
3. ~~**Decide discovery**~~ — **DONE (rec b, §0):** `simple_oauth_server_metadata`
   (via `e0ipso/simple_oauth_21`) added + enabled; discovery serves. Still to
   decide before enabling for real: **vendoring** the GitHub-only dep into
   `git.<gitlab-host>` per the threat model (see §0 supply-chain note).
4. **Client secret handling:** the generated secret is in
   `scripts/f26/.f26-moodle-client-secret` (gitignored). Move it into Moodle
   by hand; consider storing via the data-secret tier for real environments.
5. **Redirect URIs:** currently `ss-dev`/`ss2-dev` only. Prod redirect URIs are
   intentionally absent — adding them is F26 Phase 6 (2-approver, human, never CI).
6. **Rename/replace `auth/avc_oauth2`:** the shipped Moodle plugin is AVC-branded
   and has no UID-lock. `auth_nwc` supersedes it for nwc↔ss; decide migration.
7. **PKCE + scopes** on the `ss_moodle` client are set; confirm they match your
   Moodle OAuth2 service config.

## 6. Decision log

```
F26-D1  Base branch = main. F26 is auth infra independent of the ops50 editorial
        line (that work lives in the nwc profile repo on feat/ops50-legal-authoring).
        Rollback SHA 89f53c4; tag pre-f26-2026-07-09 created on it.
F26-D2  Worked in an isolated git worktree (/home/rob/nwp-f26) off main, per the
        shared-~/nwp branch-race rule — no commits in ~/nwp directly.
F26-D3  nwc↔ss shape = P72 §3.2 shape 1 (nwc is the issuer; Moodle idnumber locks
        to the nwc uid). Chosen so the landed identity is already an nwc member and
        to match the existing nwc-branded provider modules. THIS is the human-gated
        auth-graph edge.
F26-D4  Reused prior work (simple_oauth, nwc_moodle_oauth, nwc_oidc_claims, keys,
        oidc-email.sh) rather than rebuilding. Did NOT edit the nwc profile repo or
        the ss Moodle repos — all new artifacts live on the main-repo branch to
        keep this MR self-contained and to avoid polluting other feature branches.
F26-D5  Issuer changes applied to nwc-dev DB for verification only; captured as an
        idempotent script so review/rollback act on the script, not opaque state.
F26-D6  Moodle client delivered as config-only. A Moodle env exists (ss-dev/ss2-dev)
        but this build exclusively owns nwc-dev; reconfiguring a shared Moodle
        site's auth was declined as out-of-ownership + auth-surface risk.
F26-D7  Client is confidential + PKCE-required, secret generated per-client and
        gitignored. No shared-secret / bearer-in-URL / anonymous shortcuts (P72 §5).
F26-D8  Discovery (.well-known/openid-configuration) NOT added: simple_oauth 6.1.1
        core does not ship it and adding server_metadata is an un-reviewed composer
        dep. Manual Moodle endpoint entry documented instead. Flagged for operator.
        [SUPERSEDED by D11 — operator approved "all recs"; discovery now added.]
F26-D9  UID-lock decision extracted into a Moodle-free pure class so it is unit
        testable now (10/10) even though the plugin needs a Moodle env to run.
F26-D10 Flagged (not fixed) the /oauth/userinfo route collision + weak custom
        UserInfoController in prior nwc code — auth-gated, operator to resolve.
        [RESOLVED by D12 — custom route/controller dropped; native path used.]

--- recs-applied session, 2026-07-09 (operator: "F26 = all recs") -----------
F26-D11 Rec (b): OIDC discovery added. The reviewed name `drupal/
        simple_oauth_server_metadata` does NOT exist on packages.drupal.org; the
        module is the `simple_oauth_server_metadata` SUBMODULE of
        `e0ipso/simple_oauth_21` (same maintainer as simple_oauth). That project
        is GitHub-only, so installed via a VCS composer repo pinned to tag
        v1.13.0 (no-api clone). Dev-site composer only (sites/nwc/dev gitignored).
        Flagged: mirror into git.<gitlab-host> before any non-dev use.
F26-D12 Rec (a): removed the custom nwc_moodle_oauth /oauth/userinfo route +
        UserInfoController (weak loadByProperties(['value'=>token]) lookup). Native
        simple_oauth.userinfo now solely serves the path. Also removed the
        redundant guilds-writing hook in nwc_moodle_oauth.module (colliding claim
        key). Changes on nwc-profile branch feat/f26-drop-custom-userinfo.
F26-D13 Latent bug fixed to make D12 work: nwc_oidc_claims' hook had the wrong
        signature (UserInterface 2nd arg vs simple_oauth 6.x's (&$claims,&$context))
        and never ran (custom controller shadowed it). Corrected + added
        NwcOidcClaimsServiceProvider to register `guilds` in the
        simple_oauth.openid.claims allow-list (required or userinfo filters it out).
F26-D14 Side effect of D11 handled: simple_oauth_server_metadata redefines the
        simple_oauth.openid.claims parameter down to JWT registered claims, which
        dropped name/email/etc from userinfo. The ServiceProvider re-asserts the
        standard profile claims (union) so userinfo returns the full expected set.
F26-D15 Rollback anchor for the recs: tag pre-f26-recs-2026-07-09 = F26 tip
        6010aab (SHA 6010aab23341e46937a3dadff5162e3371581289). Rec work built on
        feat/f26-oidc-nwc-ss (MR !49) in the existing /home/rob/nwp-f26 worktree;
        profile-repo recs on nwp/nwc branch feat/f26-drop-custom-userinfo. MR !49
        stays OPEN — auth surface, operator review before merge/enable.
```

## 7. Enable / rollback

**Enable (after your sign-off):**
1. Re-run `scripts/f26/provision-nwc-issuer.sh` against the target nwc (dev only
   here; prod is Phase 6 / mons-gated).
2. Follow `scripts/f26/moodle/INSTALL.md` on the Moodle you own.
3. Smoke-test the browser flow (INSTALL.md §4).

**Rollback:**
- Code: `git checkout main` / reset the branch; anchor tag `pre-f26-2026-07-09`.
- nwc-dev issuer config (reversible):
  ```bash
  cd ~/nwp/sites/nwc/dev
  ddev drush cdel simple_oauth.settings public_key 2>/dev/null; true
  ddev drush cdel simple_oauth.settings private_key 2>/dev/null; true
  ddev drush php:eval '$e=\Drupal::entityTypeManager()->getStorage("consumer")->loadByProperties(["client_id"=>"ss_moodle"]); foreach($e as $c){$c->delete();}'
  ddev drush php:eval 'foreach(["openid","profile","email"] as $s){$x=\Drupal::entityTypeManager()->getStorage("oauth2_scope")->load($s); if($x)$x->delete();}'
  ddev drush cr
  ```
  (Leaves the pre-existing modules/keys untouched — they predate this build.)
- Moodle: disable the auth method + delete the OAuth2 service (INSTALL.md Rollback).

**Rollback for the applied recs (nwc-dev):**
```bash
cd ~/nwp/sites/nwc/dev
# rec (b): drop discovery module + the VCS composer dep
ddev drush pmu simple_oauth_server_metadata simple_oauth_21 -y
ddev composer remove e0ipso/simple_oauth_21
ddev composer config --unset repositories.simple_oauth_21
# rec (a): restore the custom userinfo route/controller + old hooks
cd ~/nwp/sites/nwc/dev/html/profiles/custom/nwc
git checkout feat/ops50-legal-authoring    # or the pre-rec profile SHA e552f05
ddev drush cr
```
The rec (a) code lives on nwp/nwc branch `feat/f26-drop-custom-userinfo`
(base `e552f05`); rec docs/scripts on `feat/f26-oidc-nwc-ss`
(anchor `pre-f26-recs-2026-07-09`).
```
