# NWP Console — deliberately tiny (single uvicorn worker on the console host).
fastapi>=0.115,<1
uvicorn>=0.30,<1
jinja2>=3.1,<4
itsdangerous>=2.1,<3
webauthn>=2.5,<3
python-multipart>=0.0.9,<1
# WS protocol support for uvicorn — the Sessions tab's terminal websocket.
websockets>=12,<16
