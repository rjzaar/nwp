# Test Secrets File
# Mock secrets for BATS tests - NO REAL CREDENTIALS
# This file is a fixture - NOT for production use

# Mock Linode API (returns predictable test values)
linode:
  api_token: "test-linode-token-12345"

# Mock Cloudflare API
cloudflare:
  api_token: "test-cloudflare-token-67890"
  zone_id: "test-zone-id-abcdef"

# Mock B2 storage
b2:
  account_id: "test-b2-account"
  app_key: "test-b2-key"

# Mock GitLab server
gitlab:
  server:
    domain: "git.test.example.com"
    ip: "192.0.2.100"
    linode_id: "12345678"
  api_token: "test-gitlab-token-xyz"
  composer_url: "https://git.test.example.com/api/v4/group/1/-/packages/composer/packages.json"

# Mock GitHub
github:
  api_token: "test-github-token-abc"

# Mock development defaults
dev_defaults:
  drupal:
    admin_user: testadmin
    admin_password: testpass123
    admin_email: admin@test.example.com

  moodle:
    admin_user: testadmin
    admin_password: testpass123
    admin_email: admin@test.example.com

  smtp:
    host: "localhost"
    port: 1025
    username: ""
    password: ""

# Mock third-party services
services:
  sendgrid_key: "test-sendgrid-key"
  ga_tracking_id: "UA-TEST-123"
