# Local Environment Overrides
# Copy this file to .env.local in your site directory
# The .env.local file is excluded from git

################################################################################
#                              LOCAL OVERRIDES                                 #
################################################################################

# Local development URL override
# VORTEX_LOCALDEV_URL=mysite.ddev.site

# Database overrides (if using external database)
# DATABASE_HOST=localhost
# DATABASE_NAME=my_database
# DATABASE_USER=my_user
# DATABASE_PASSWORD=my_password

# Admin credentials (for automated provisioning)
# DRUPAL_ADMIN_USER=admin
# DRUPAL_ADMIN_PASSWORD=admin
# DRUPAL_ADMIN_EMAIL=admin@localhost

# Debug settings
# ENV_DEBUG=1
# XDEBUG_ENABLE=1
# XDEBUG_MODE=debug,develop

# Stage file proxy (point to production site)
# STAGE_FILE_PROXY_ORIGIN=https://www.example.com

# API Keys and Tokens (keep secret!)
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxx
# COMPOSER_AUTH={"github-oauth": {"github.com": "xxxxx"}}

# Service overrides
# REDIS_ENABLED=1
# SOLR_ENABLED=1

# Deployment credentials
# DEPLOY_TARGET=user@production.example.com:/var/www/sitename
# DEPLOY_SSH_KEY=~/.ssh/id_rsa

# Email settings for testing
# SMTP_HOST=localhost
# SMTP_PORT=1025
# SMTP_USER=
# SMTP_PASSWORD=

# Performance settings for local development
# PHP_MEMORY_LIMIT=512M
# PHP_MAX_EXECUTION_TIME=300
